# MergePilot · 初赛声明—证据矩阵

> **历史快照说明。** 本文件记录 2026-08-11 的初赛声明与证据映射。当前公开入口与真实性边界见仓库根目录的 [README](../README.md)；材料用途和时效见 [文档索引](README.md)。下方的 D2B-3 与 `hiclaw_live=true` 只描述该日期的验证记录，不构成当前能力声明。

> **⚠ 初赛提交快照（2026-08-11）**：以下为初赛提交时的历史结论。D2B-3 已于 2026-08-11 通过真实 AgentTeams v1.2.2 生产 live 验证（`hiclaw_live=true`，64/64 PASS）。最新状态见 [README.md](../README.md)、[docs/项目状态.md](项目状态.md)、[权威 evidence](../evidence/m5/0d/hiclaw-v122-true-live-pass.json)。

> HEAD = `4b053efc3cb16fe4558143a1c7bbdd78e2b18073` | origin/main = `4b053ef` | 2026-08-11

本文件逐项列出初赛可公开声明及其权威证据。评审可在 [证据索引](初赛证据索引.md) 中 3 分钟内定位每项。

---

## 一、已验证声明（机器可验、有 evidence、有 tag）

| # | 对外声明 | 状态 | commit/tag | evidence 路径 | 关键数字 | 不得声称的边界 |
|---|---|---|---|---|---|---|
| 1 | 可靠状态机 + Outbox + L2 审批 + 故障恢复/回滚 | 已验证 | `m3b-b4e-closed` / `m3c-closed` | `evidence/m3b-b4e/` `evidence/m3c/` | B4e 43/43、B5 50/50、M3-C 33/33 | 回滚仍为脚本触发，非 verify-fail 自动分支（M3-C 已实现 child-run 回滚但仅限 POST_MERGE_VERIFY_FAILED 入口） |
| 2 | 最小权限 Policy Gateway（角色 token + 写约束 + audit） | 已验证 | `m3b-b1-closed` → `m3b-b5-closed` | `evidence/m3b-b5/` | B5 50/50（8 类负向全 fail-closed） | Gateway 为自研 Python SSE，非 Higress 原生 |
| 3 | 6 Skill DAG（diff-parse / risk-classify / sast-scan / test-runner / pr-lifecycle / case-retrieval） | 已验证 | `m4a`–`m4e` tags | `evidence/m4/m4c/` `evidence/m4/m4e/` | 75+96+87+54+169=481 确定性测试 | Skill 为确定性子进程，非 LLM 自主调用 |
| 4 | AgentTeams 全链协议 E2E | 已验证 | `m4f-agentteams-demo-closed` | `evidence/m4/m4f/` | 16/16 门禁 + 6/6 legacy 回归；6 Skill 全 SUCCEEDED | hiclaw_live=false（协议 fixture，非真实 HiClaw 线上） |
| 5 | 真实 GitHub MCP（PR 审查→修复→验证→合并） | 已验证 | 早期 demo + M3/M4 | `docs/项目状态.md 第二节（早期 demo，路径见原文）` | PR #1 → PR #3 squash merge；5/5 resolved | 早期 PR #1/#3 demo 阶段交接曾需人工 nudge；M5-0B 候选工作流已确定性闭环（14/14+13/13），仅限候选/隔离栈，不可外推为生产 HiClaw 零人工 |
| 6 | 结构化审计（PG16 + pgvector） | 已验证 | M3/M4 闭合 | `evidence/audit-db/（见 docs/项目状态.md 第四节）` | 5 tasks / 6 findings / 3 decisions / 9 audit events | 未连接 PolarDB 云实例；本地容器 |
| 7 | RAG CaseRetrieval 核心 | 已验证 | `m4e-case-retrieval-closed` | `evidence/m4/m4e/` | 169 测试；真实 pgvector Docker E2E `all_passed=true` | Agent 可调用 HTTP/MCP 封装仍待完成；5 条小样本不代表生产精度 |
| 8 | HiClaw 隔离 C3 十轮稳定性 | 已验证 | `4b053ef`（未 tag） | `evidence/m5/0c/c3-10x.json` | **10/10 PASS**，source_commit=`4b053ef` | MergePilot-Test 隔离测试栈（非生产 Ubuntu-22.04）；hiclaw_live=false |
| 9 | D2B-1 离线回归 | 已验证 | `4b053ef`（未 tag） | `evidence/m5/0d/offline-regression.json` | **17/17 + 6/6**，source_commit=`4b053ef` | 离线回归不含 OTel/SLS 生产证据 |
| 10 | 存储防膨胀代码 + guarded startup | 候选/部分 | `3c96701` + `4b053ef` | `tools/hiclab/`（240 单测） | 240 测试；生产部署 capability preflight 验证 PASS | Manager auto-create 路径仍 BLOCKED_UPSTREAM（无 socket proxy） |
| 11 | 正式 Benchmark N=10×2（单 Agent vs MergePilot 多角色） | 已冻结（post-run 机器复算） | HEAD `4b053ef`（freeze 工具 + 产物） | `benchmark/formal-summary.json` `benchmark/formal-summary.md` `benchmark/formal-run-manifest.json`；20 raw runs：`benchmark/raw-runs/` | infrastructure 20/20；A precision/F1=36.36%/48.00%，B precision/F1=57.14%/63.16%，recall 同为 70.59%；B token 高 32.95%、API 请求 +80% | 受控本地编排（controlled local orchestration），**非真实 Gateway/controller/GitHub/HiClaw E2E**；N=10 小样本；单模型 deepseek-v4-flash；synthetic fixtures；每 pair 单次运行；不证明多角色提高 recall（recall 相同）；不支持真实 fix/verify/rollback 指标；**不可与 C3 10/10 混为一项** |

## 二、明确限制（不得宣称）

| # | 限制 | 精确状态 |
|---|---|---|
| A | **hiclaw_live = false** | 22 项公式中 5 true / 0 false / 17 unproven；缺少生产 HiClaw live 窗口 |
| B | **D2B-3 BLOCKED_UPSTREAM** | HiClaw controller 无 disable-auto-create 配置（审计确认）；无 socket proxy |
| C | **D2B-2 OTel/SLS 缺口** | OTel/SLS 生产证据未采集；官方 Skill 未集成 |
| D | **Nacos / RocketMQ 未接入** | 规划中，代码未实现 |
| E | **Socket proxy 未部署** | harden_policy.py 策略已实现+测试，但 daemon 未部署 |
| F | **Benchmark 为受控本地评测** | N=10×2 已冻结并机器复算；但非真实生产 E2E；N≥20 仍为后续目标；semantic case pass 仅 5/20（A=2、B=3），不等于 E2E 完成率；不证明 recall 提升 |
| G | **Manager 阶段交接偶需人工** | 非"一次提交零干预" |
| H | **SAST 存在新旧两条路径** | 新版 `skills/sast_scan/`（87 测试、schema、fail-closed、1 MiB）与旧版 `skills/sast-scan/scan.py`（早期 Reviewer/SOUL/demo 路径）并存；新版属性不得外推到旧版；统一为初赛后技术债 |
| I | **MIG-B4-001 未解决** | B4 链迁移非幂等（早版 `m3b_b4.sql` 无法 CREATE OR REPLACE 回退）；支持路径=forward-only；M7 空环境复现前需正式 migration runner |

## 三、候选/部分完成（可谨慎声明"已实现核心，候选/部分完成"）

| 项 | 已完成 | 未完成 |
|---|---|---|
| HiClaw 存储防膨胀 | disk guard + guarded startup + cleanup tooling + capability preflight（代码 + 部署验证） | socket proxy 未部署；Manager 路径未根治 |
| Guarded startup | 6 容器 restart=no + systemd service + drop-in + capability preflight（生产部署 + 验证） | mergepilot-controller 健康探针仍用 running_uptime（弱） |
| M5-0 HiClaw Live | M5-0A/B 已发布；C3 10/10 + D2B-1 17/17 在隔离测试栈 | hiclaw_live=true 尚未达成（需生产窗口） |
