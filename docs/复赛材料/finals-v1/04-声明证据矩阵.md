# 声明证据矩阵

冻结：`v0.1.0-preview.4` / `5bb2635`；同机验收：`SAME_MACHINE_ACCEPTED`；外部验收：`EXTERNAL_BLOCKED`。

| 声明 | 状态 | 证据 | 限制 | 口播 |
|---|---|---|---|---|
| 4 个运行时 Agent 承载 6 类职责 | 已实现 | `config/souls/`、`tools/agentteams/` | 非 6 个已部署 Agent | 是 |
| 6 类 Skill DAG | 已实现/已验证 | `skills/`、M9 门禁 | RAG advisory | 是 |
| Controller 确定性交接与 fail-closed | 已实现/部分验证 | `tools/workflow-controller/`、M9 evidence | 完整生产集成未验证 | 是，带边界 |
| Gateway + GitHub MCP 隔离服务 | 已实现 | `tools/policy-gateway/`、`config/m5-0c/` | PAT/生产外部环境未验证 | 是 |
| 9 镜像离线包、约 847MB | 已验证 | 正式 Release manifest、SHA 清单 | 包体较大 | 是 |
| Standalone Install→Doctor | 已验证 | `m9k-final-candidate-evidence`（测试机证据目录） | 同机 | 是 |
| 页面/API 200，405/404/403 | 已验证 | M9-K lifecycle evidence | loopback-only | 是 |
| Stop/再 Start/双 Cleanup | 已验证 | M9-K cleanup evidence | 正式包含 9 个镜像；Cleanup 按 manifest 删除其中 8 个属主镜像。测试机预置的 3 个非属主镜像属于包外基线，fail-closed 保留；Docker 探针有瞬态噪音 | 是，说明所有权边界与噪音 |
| 只读控制台 | 已验证 | M9-K screenshot | 无 RBAC、非管理后台 | 是 |
| 门禁 2471 passed / 20 skipped | 已验证 | merged-main 门禁记录 | skipped 保留原语义 | 是 |
| 独立 Windows 物理机验收 | 待验证 | `EXTERNAL_BLOCKED` | 无独立机器证据 | 否 |
| 正式受控评测 20×2×3，120/120 Schema 合规 | 已验证（受控评测） | `benchmark/preview4-refresh-formal-20260826/summary.json` + `material-claims.json`；benchmark commit `7831fbf` | 合成 fixture；非真实全链；CI 不作显著性宣称 | 是，带边界 |
| Precision/F1/Decision acc/Recall 及成本数字 | 已验证（受控评测） | material-claims.json（由 120 raw runs 程序复算） | deepseek-v4-flash 版本一致性 NOT_CONFIRMED；不可与 2026-08-11 基线直接归因比较 | 是 |
| Benchmark 产品耦合 = Skill(sast_scan/risk_classify) + SOUL | 已实现 | `benchmark/adapters/` + `benchmark/preview4_refresh/`；canonical-lf-v2 身份 | Controller/Gateway/GitHub MCP/真实 E2E 未参与（NOT_VERIFIED） | 是 |
| production ready / production verified | 不可声明 | `production_verified=false` | Preview 边界 | 否 |

## 固定边界

`application_integration_verified=false`  · `database_verified=false`  · `production_verified=false`  · `revision_producer_contract=NOT_VERIFIED`  · `audit_producer_contract=NOT_VERIFIED`  · `direct_routing_verified=false`  · `transport_profile=wsl-user-relay`
