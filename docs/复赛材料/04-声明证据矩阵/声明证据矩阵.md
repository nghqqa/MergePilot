# MergePilot复赛声明证据矩阵

冻结版本：`v0.1.0-preview.3` / `379744d`。

| PPT | 声明 | 状态 | Commit / Tag | 测试或证据 | 限制 | 允许口播 |
|---:|---|---|---|---|---|---|
| 1 | MergePilot提供多Agent PR治理控制面 | 已实现 | `379744d` | `README.md`，核心模块 | Preview，不是生产系统 | 是，带Preview边界 |
| 2 | 六逻辑角色收敛为四运行时Agent承载六类职责 | 已实现 | `379744d` | `config/souls/`，`tools/agentteams/` | 架构演进，不是六个已部署Agent | 是 |
| 3 | Controller / Gateway处理确定性治理 | 已验证 | M3 / M4 tags | `evidence/m3b-b4e/`，`evidence/m3b-b5/` | 各证据环境需单独陈述 | 是 |
| 4 | 当前链路为Controller -> AgentTeams -> Skill -> Gateway -> MCP | 已实现 | `379744d` | `tools/workflow-controller/`，`tools/policy-gateway/` | 不代表每条外部集成均生产验证 | 是 |
| 5 | 四个运行时Agent为Manager、Reviewer、Fixer、Verifier | 已验证 | M8证据链 | `config/souls/`，真实handoff记录 | 外部运行时可能需要恢复提醒 | 是，带边界 |
| 6 | 六类Skill具有工程合同 | 已验证 | M4 tags | `evidence/m4/m4f/verification.txt` | RAG为advisory | 是 |
| 7 | PostgreSQL状态机、Outbox和幂等推进 | 已验证 | `m3b-b4e-closed`等 | 43/43、50/50、33/33历史证据 | `revision_producer_contract`未验证 | 是 |
| 8 | Worker不持PAT，GitHub写入经过Gateway和MCP | 已验证 | M3/M5证据 | policy配置、Gateway负向矩阵 | 不展示真实凭证 | 是 |
| 9 | 本地Trace / OTel与结构化审计存在 | 已验证/部分待验证 | M6-A及当前main | local collector证据、audit-db | 云SLS未完成；audit producer未验证 | 是，逐项说 |
| 10 | 系统具备fail-closed与回滚链 | 已验证 | M3-C、M8 | rollback evidence、harness测试 | 不概括为生产所有场景自动回滚 | 是，带环境 |
| 11 | run35完成17阶段、16项前置、6条边 | 已验证 | run35冻结SHA | workspace evidence bundle `temp/m8gh4-run27/evidence/` | 本地真实E2E，不是生产 | 是 |
| 11 | Receipt与Matrix在run35验证 | 已验证 | run35冻结SHA | session journal、receipt、status projection | 不翻转五项真实性边界 | 是 |
| 12 | 控制台为GET-only只读证据面 | 已验证 | `03996cd`及后续 | 29项console测试、浏览器截图 | 无RBAC、无写操作、非生产管理后台 | 是 |
| 12 | Complete / Failed / Stale诚实渲染 | 已验证 | `379744d` | 投影测试与浏览器smoke | failed fixture须标生产函数派生 | 是 |
| 13 | preview.3同机安装和生命周期验收通过 | Preview验收 | `v0.1.0-preview.3` | `SAME_MACHINE_ACCEPTED`，2246/20 | `EXTERNAL_BLOCKED` | 是，必须同句说明 |
| 13 | 9镜像离线包为847.4MB | 已验证 | Release manifest | GitHub Release 4资产 | 包体较大 | 是 |
| 14 | N=10x2中precision和F1提高、FP下降 | 离线验证 | benchmark冻结commit | `benchmark/formal-summary.md` | 单模型、合成fixture、小样本 | 是，必须同时说代价 |
| 14 | 多Agent提升决策准确率 | 不可声明 | - | Decision accuracy 50% -> 40% | 数据不支持 | 否 |
| 14 | RAG提高Reviewer/Fixer准确率 | 不可声明 | - | `runtime_consumes_rag_context=false` | 当前无法测量工作流效用 | 否 |
| 15 | 独立Windows物理机验收完成 | 待验证 | - | `EXTERNAL_BLOCKED` | 尚未执行 | 否 |
| 15 | 生产可用或生产验证完成 | 不可声明 | - | `production_verified=false` | Preview不等于生产 | 否 |

## 跨材料固定边界

```text
application_integration_verified=false
database_verified=false
production_verified=false
revision_producer_contract=NOT_VERIFIED
audit_producer_contract=NOT_VERIFIED
direct_routing_verified=false
transport_profile=wsl-user-relay
```
