# MergePilot · 初赛证据索引（评审快速定位）

> **⚠ 初赛提交快照（2026-08-11）**：以下为初赛提交时的历史证据索引。D2B-3 已于 2026-08-12 通过真实 AgentTeams v1.2.2 生产 live 验证（`hiclaw_live=true`，64/64 PASS）。最新状态见 [README.md](../README.md)、[docs/项目状态.md](项目状态.md)、[权威 evidence](../evidence/m5/0d/hiclaw-v122-true-live-pass.json)。

> HEAD = `4b053ef` | 2026-08-11 | 评审目标：3 分钟内定位任意声明对应的证据

## 核心架构

| 要点 | 证据入口 |
|---|---|
| 8 段闭环架构图 | `README.md`（架构段） |
| 数据库契约（5 表 + CHECK + 函数） | `tools/audit-db/m3_state.sql` + `m3b_b4.sql` + `m3b_b4c1.sql` |
| 状态机转换图 | `docs/M3-B4-审批票据与Action-Outbox设计.md` |
| Agent Identity 清单 | `docs/附录A-Agent-Identity清单.md` |
| Skill 清单 | `docs/附录B-Skill清单.md` |

## 正向 E2E

| 阶段 | 结果 | 证据路径 |
|---|---|---|
| B4e 总 E2E (review→fix→verify→approve→drain→MERGED) | **43/43** | `evidence/m3b-b4e/README.md` |
| M3-C 回滚 E2E (child-run revert→reverify) | **33/33** | `evidence/m3c/README.md` |
| M4-F AgentTeams 全链 (controller→Gateway→MCP→6 Skill→PRLifecycle) | **16/16** | `evidence/m4/m4f/verification.txt` |
| C3 十轮稳定性 (c2_smoke×10) | **10/10** | `evidence/m5/0c/c3-10x.json` |

## 负向 / Fail-Closed

| 场景 | 结果 | 证据路径 |
|---|---|---|
| B5 负向门 (直连拒/跨角色/伪造/过期/重复) | **50/50** | `evidence/m3b-b5/README.md` |
| Skill secret redaction + fail-closed | 0 leaks | `evidence/m4/m4c/` |
| Gateway DENY 反例 (non-fixture/nonfix/main push) | 全 DENY | `evidence/m4/m4f/full-chain-e2e.json` |

## 权限与密钥隔离

| 要点 | 证据路径 |
|---|---|
| GitHub PAT 仅在 github-mcp (mcp-backend-net 隔离) | `evidence/m3b-b4e/` `docs/项目状态.md 第二节（早期 demo，路径见原文）` |
| 角色 token + 写约束 (fixer 仅 fixture repo) | `docs/M5-0-HiClaw-Live设计冻结.md` §16 §26.4 |
| secret scan = 0 hits | 所有 evidence `secret_leaks=0` |

## 故障恢复 / 回滚

| 场景 | 结果 | 证据路径 |
|---|---|---|
| lease 崩溃恢复 + UNKNOWN/EXECUTING 对账 | PASS | `evidence/m3b-b4e/README.md` |
| Gateway 降级→恢复 (circuit breaker) | PASS | `evidence/m3b-b4e/README.md` |
| verify-fail 回滚 (revert commit → 复验 PASS) | PASS | `evidence/m3c/README.md` |

## Skill 测试

| Skill | 测试数 | tag | evidence |
|---|---|---|---|
| diff-parse + risk-classify | 96 | `m4b-diff-risk-closed` | `evidence/m4/m4b/` |
| sast-scan + test-runner | 87 | `m4c-sast-test-closed` | `evidence/m4/m4c/` |
| pr-lifecycle | 54 | `m4d-pr-lifecycle-closed` | `evidence/m4/m4d/` |
| case-retrieval (RAG) | 169 | `m4e-case-retrieval-closed` | `evidence/m4/m4e/` |
| AgentTeams 全链集成 | 16/16+6/6 | `m4f-agentteams-demo-closed` | `evidence/m4/m4f/` |

## C3 + D2B-1

| 项 | 结果 | source_commit | evidence |
|---|---|---|---|
| C3 十轮稳定性 | **10/10 PASS** | `4b053ef` | `evidence/m5/0c/c3-10x.json` |
| D2B-1 离线回归 | **17/17 + 6/6** | `4b053ef` | `evidence/m5/0d/offline-regression.json` |

## 正式 Benchmark（N=10×2，冻结）

| 项 | 结果 | source_commit | evidence |
|---|---|---|---|
| Benchmark source manifest（24 个 source/fixture digest） | SHA256=`0de4917e…` | `4b053ef` | `benchmark/source-manifest.json` |
| 20 raw runs combined SHA256 | `c863ecd6…` | `4b053ef` | `benchmark/raw-runs/*.json`（10 cases × 2 groups） |
| Formal summary（机器复算） | infrastructure 20/20；A prec/F1=36.36%/48.00%，B prec/F1=57.14%/63.16%，recall 同为 70.59% | `4b053ef` | `benchmark/formal-summary.json` `benchmark/formal-summary.md` |
| Formal run manifest（head + 全文件 SHA + totals） | tokens=28099、API=28、key_absent=true、external_environment_used=false | `4b053ef` | `benchmark/formal-run-manifest.json` |
| Secret pattern 扫描 | synthetic_fixture_pattern_hits=2、real_credential_hits=0 | `4b053ef` | `benchmark/formal-summary.json`（§secret_pattern_scan） |

> 受控本地评测（controlled local orchestration），非真实 Gateway/controller/GitHub/HiClaw E2E；N=10 小样本、单模型、synthetic fixtures、每 pair 单次运行；不证明多角色提高 recall（两组 recall 相同）；**不可与 C3 10/10（独立真实隔离栈）混为一项**。

## 当前已知限制（评审须知）

1. **hiclaw_live = false**：22 项公式 5 true / 17 unproven（未达成生产 HiClaw live 窗口）
2. **D2B-3 BLOCKED_UPSTREAM**：HiClaw 无 disable-auto-create 配置（审计确认）；无 socket proxy
3. **OTel/SLS 未实现**：D2B-2 缺口；无生产可观测性证据
4. **Nacos / RocketMQ 未接入**
5. **人工干预**：早期 PR #1/#3 demo 曾需人工 nudge；M5-0B 候选工作流已确定性闭环（14/14+13/13），仅限候选/隔离栈，不可外推为生产零人工
6. **存储防膨胀**：代码 + 部署已验证（240 单测 + capability preflight），但 Manager 路径未根治（需 proxy）
7. **Benchmark 为受控本地评测**：N=10×2 已冻结机器复算；但非真实生产 E2E；semantic case pass 仅 5/20，不等于 E2E 完成率；不证明 recall 提升；N≥20 仍为后续目标
8. **SAST 新旧两路径并存**：新版 `skills/sast_scan/`（87 测试）与旧版 `skills/sast-scan/scan.py`（Reviewer/SOUL/demo）并存；新版属性不外推到旧版；统一为初赛后技术债
9. **MIG-B4-001**：B4 链迁移非幂等；M7 空环境复现前需正式 migration runner

## 评审 3 分钟速查路径

```
1. README.md              → 整体架构 + 差异化定位（30s）
2. evidence/m3b-b4e/      → 完整 E2E 闭环（30s）
3. evidence/m5/0c/        → C3 十轮稳定性（15s）
4. evidence/m4/m4f/       → 六 Skill DAG 全链（30s）
5. evidence/m3b-b5/       → 负向 fail-closed（15s）
6. benchmark/formal-summary.md → 正式 Benchmark 冻结结论（30s）
7. docs/初赛声明-证据矩阵.md → 逐项声明 vs 限制（30s）
```
