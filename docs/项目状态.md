# MergePilot · 项目状态总览

> **历史状态记录。** 本文保留截至 2026-08-11 的里程碑与运行结论，不是当前公开能力的唯一来源。请先阅读仓库根目录的 [README](../README.md)，并通过 [文档索引](README.md) 选择当前设计、历史证据或验证约定。M8 整体尚未完成；M8-A1 的事件接入机制不等于 revision producer integration。M8-A2-a 已于 2026-08-17 通过隔离六容器 fixture 验证（11 PASS / 0 FAIL）。M8-A2-b 于 2026-08-18 完成一次性本地收口：A2-b1 38 + A2-b2 25 = 63 项隔离测试；在本机真实环境（nghqqa/MergePilot-e2e-fixture 真实 PR、真实 github-mcp + Policy Gateway 审计（run_id+git_sha provenance 行）、真实 hiclab Matrix homeserver、隔离 PostgreSQL 上应用既有 m4f1 迁移）零种子跑通 TASK_SUBMITTED → M4F_RUN → Gateway → bind_revision → snapshot → 六 Skill DAG 入队；负面（严格解析失败、保留前缀拒绝、Candidate 模式下 admin 发 M4F_RUN 被拒）与幂等（task_runs/stage_runs/dispatch 各 1 行无重复）实测通过。真实 AgentTeams Manager LLM producer 未演示（LLM API key 失效 401；运行时已验证至 LLM 调用：房间加入、mention 唤醒、应答管道）；五项真实性边界维持不提升。附带修复：candidate 模式 /sync 过滤器现透传 TASK_SUBMITTED（生产 process_event 仍要求 sender=admin 且幂等），并发现部署侧 gateway 容器代码落后于 HEAD 导致审计缺 run_id/git_sha（已热替换验证）。**同日 A2-b3 重跑（LLM 凭据修复后）**：真实 Manager（hiclab v1.2.2 内 DeepSeek/OpenClaw agent，经 aigw）在 operator DM 指令下逐字转发 M4F_RUN 契约消息（event `$OnqSpT…`，byte-exact 过 Candidate 严格解析器），run `m8a2m-r3` 于 fixture PR #624 上闭合全链——stage_events sender=@manager、真实 Gateway provenance 行、bind_revision（base=20e92b1c…/head=03e07536… 与 PR 一致）、snapshot SUCCEEDED、六 Skill DAG 入队。诚实边界：producer 动作是 operator 指令下的 LLM 自主转发，不是自主任务分解；Worker handoff 回路（reviewer/fixer/verifier TASK_COMPLETED）未在真实环境演示；五项真实性边界仍不提升。

> 最后更新：2026-08-11（HEAD=f1c230e）。本文区分”已验证””框架自带/环境能力”和”规划”，不把后续目标写成既成事实。

## 〇、D2B-3 PASSED — 权威状态（2026-08-11）

**D2B-3 fail-closed Docker socket proxy 已在真实 AgentTeams v1.2.2 生产环境验证通过。**

| 维度 | 值 |
|---|---|
| upstream_version | **v1.2.2** |
| upstream_source_commit | **`849182af8e017168a5a200a87b1062142caf462d`** |
| proxy_source_commit | **`e984ef394ce80e3572159f9ebed154518d7565e4`** |
| production live | **64/64 PASS, `hiclaw_live=true`** |
| roles_tested | reviewer / fixer / verifier / manager |
| name_profile | agentteams (v1.2.2 naming) |
| 权威 evidence | [`evidence/m5/0d/hiclaw-v122-true-live-pass.json`](../evidence/m5/0d/hiclaw-v122-true-live-pass.json) |

兼容 evidence（v1.1.2 镜像 + v1.2.2 proxy）：`hiclaw-v122-live-verify.json`（commit `6e90086`），标记为 “v1.2.2 Proxy + v1.1.2 upstream compatibility live”。

**D2B-3 `BLOCKED_UPSTREAM` 状态已解除**。Socket proxy 已实现、部署并通过真实 v1.2.2 生产 live 验证。详见 [`tools/hiclab/UPSTREAM_BLOCKED.md`](../tools/hiclab/UPSTREAM_BLOCKED.md)（状态=PASSED）。

## 一、当前结论（初赛阶段快照，2026-08-11）

> **注**：以下为初赛提交时的历史快照。最新状态见上方”〇、D2B-3 PASSED”。

**初赛材料已按最新项目进展完成同步，核心原型已从 fixture 双场景推进到真实 GitHub MCP、回滚执行链、本地结构化审计与 RAG。**

| 维度 | 当前结果 |
|---|---|
| 职能 / 运行时 | 6 类职能；4 个运行 Agent（Manager + 3 Worker） |
| 自研 Skill | 11 项设计；M4-A/B/C/D/E 五项核心 Skill 已发布 |
| 官方 Skill | `alibabacloud-sls-query` 规划 |
| 真实 GitHub | PR #1 审查 → 修复 PR #3 → 5/5 resolved → 合并 |
| 回滚 | FAIL → revert commit → 复验 PASS；当前脚本触发 |
| 审计库 | 本地 PostgreSQL 16 + pgvector；5 tasks / 6 findings / 3 decisions / 9 audit events |
| RAG | 5 条案例；5 组查询 top-1 均命中，余弦 0.830–0.962 |
| 双场景 | PR #42 MERGE（人审后）；PR #43 HOLD / Do not merge |
| 基准证据 | 40/40 检查；18 份证据产物 |

## 二、真实 GitHub MCP（已验证）

仓库：`nghqqa/mergepilot-test`。

| 阶段 | 结果 | 证据 |
|---|---|---|
| Reviewer | 读取 PR #1，结合 `sast-scan` 产出 6 findings | `evidence/gh-pr1-demo/tasks/gh-pr1-review/findings.md` |
| Fixer | 建分支、写文件、创建真实 PR #3 | `evidence/gh-pr1-demo/tasks/gh-pr1-fix/result.md` |
| Verifier | 5/5 可修复项解决，0 新问题，PASS | `evidence/gh-pr1-demo/tasks/gh-pr1-verify/verify-report.md` |
| Merge | PR #3 squash 合并，merge commit `0dd5831` | GitHub PR #3 + audit-db |

安全属性：GitHub PAT 只存在 `github-mcp` 凭证 sidecar；Worker 经 mcporter 调用，不持有 PAT。

**当前边界**：早期 PR #1/#3 demo 的 Manager 编排阶段交接曾需人工 nudge。M5-0B 候选工作流已通过 reconcile_m5_skill_to_review/reconcile_m5_handoffs/advance_m5_review_run 实现确定性闭环（14/14 handoff integration + 13/13 concurrency/negative），但仅限候选/隔离栈。~~由于 production-live evidence 缺失（hiclaw_live=false）~~ → **D2B-3 已通过真实 v1.2.2 生产 live（hiclaw_live=true，64/64 PASS）**。

## 三、回滚执行链（已验证）

- 坏修复 commit：`43eccc3`。
- `sast-scan` 复验：FAIL（硬编码密钥/SQL 注入）。
- 回滚 commit：`a63bfe1`。
- 回滚后复验：0 findings，PASS。
- GitHub 写入均经 MCP 完成。

**当前边界**：回滚由 `rollback-release-candidate.sh` 显式触发；下一阶段将其固化到 Manager 的 verify-fail 自动分支。

## 四、结构化审计与 RAG（已验证核心）

### 审计库

- 本地 `pgvector/pgvector:pg16` 容器，5 张业务表 + `knowledge` 表。
- 已写入 5 tasks、6 findings、3 decisions、9 audit events，事件包含 agent/action/target/SHA/via/time。
- 当前未连接 PolarDB 云实例；协议、SQL 和 pgvector 结构兼容 PolarDB-PG，迁移主要切换连接配置。

### CaseRetrieval / rag-recall

- **M4-E CaseRetrieval Skill 已发布**（`m4e-case-retrieval-closed`，peeled `621e3aa`）：框架中立只读 RAG 检索，169 测试；Windows 166/3×2、POSIX 158/11；真实 pgvector Docker fixture E2E `all_passed=true`。详见 [M4 进展](#九m4-进展实时权威以标签为准)。
- 早期原型 `tools/rag/rag.py`：2026-07-24 重跑英文三类 top-1 0.900–0.962、中文两类 0.830–0.855；5 组均命中。该结果为 5 条小样本确定性验证，不代表生产数据集通用准确率。
- 部署边界：repo scope/DSN/model/timeouts 全部 deploy-owned；reader 为只读非特权身份；历史案例 `untrusted` 恒为 true，不授权 merge、不替代当次 SAST/TestRunner。
- Agent 可调用的 HTTP/MCP 服务封装仍待完成（下一主线）。

## 五、双场景与首轮证据（继续保留）

- PR #42：6 项 findings，中低危修复后通过复验，高危保留人工审批，最终 `MERGE（人审后）`。
- PR #43：4 项 findings，确认 3+ 已知 CVE，最终 `HOLD` 并明确 `Do not merge`。
- 首轮基准：40/40 检查通过，18 份审查/修复/验证/Trace/补丁证据。

## 六、当前提交材料

| 材料 | 状态 |
|---|---|
| 作品简介 | 467 个非空白字符，≤500 |
| 方案 PPT | `MergePilot-初赛方案-美化版-v4.pptx`，17 页 |
| PDF | `MergePilot-初赛方案.pdf`，17 页，与 v4 对齐 |
| 附录 A | 6 类 Agent Identity；明确 4 Agent 运行时映射 |
| 附录 B | 11 自研 + 1 官方规划；逐项标注已验证/规划 |
| 原型代码包 | GitHub MCP、回滚、audit-db、RAG、SOUL、SAST、复现脚本 |
| 证据看板 | gh-pr1-demo、rollback-demo、audit-db、rag + 自包含 HTML |

## 七、下一阶段（V0.5）

1. 将阶段 handoff 固化为零 nudge 的 Manager 状态机。
2. ~~将回滚脚本纳入 verify-fail 自动触发分支。~~ ✅ 已由 M3-C 完成（`POST_MERGE_VERIFY_FAILED` → revert child run → ROLLED_BACK → reverify；见 `m3c-closed`）。
3. 接入 CI、真实 SLS 查询 Skill 与 AgentLoop/OTel。
4. 引入 Nacos/RocketMQ 做治理与事件可靠性。
5. 将 RAG 封装为 Agent 可调用 HTTP/MCP 服务，并扩充评测集。
6. ~~执行 N=10×2 正式 Benchmark 并冻结量化结论。~~ ✅ 已于 2026-08-11 完成（受控本地评测；post-run 机器复算冻结；见下文本节"正式 Benchmark 冻结"）。

### 正式 Benchmark 冻结（2026-08-11，N=10×2，post-run 机器复算）

- **冻结产物**：`benchmark/formal-summary.json`、`benchmark/formal-summary.md`、`benchmark/formal-run-manifest.json`、`benchmark/freeze_formal_results.py`（只读、原子写、幂等、fail-closed）。
- **基础校验**：raw-runs 恰好 20 个、`(bm-01..bm-10) × (A_single_agent/B_mergepilot)` 精确各一次、20/20 schema valid、model=deepseek-v4-flash、status 全 completed、error_detail 全空；source-manifest 24/24 digest 匹配、SHA256=`0de4917e…`；raw combined SHA256=`c863ecd6…`。
- **infrastructure completion = 20/20（100%）**：受控本地编排为每个 (case × group) pair 产出了可解析、schema 合法、completed 的 run。
- **semantic case pass = 5/20（A=2、B=3）**：注意 semantic case pass 要求 per-case decision 与 finding-level 评估同时正确，**不是 E2E 完成率**。
- **finding-level 指标**：

  | group | TP | FP | FN | precision | recall | F1 |
  |---|---:|---:|---:|---:|---:|---:|
  | A_single_agent | 12 | 21 | 5 | 36.36% | 70.59% | 48.00% |
  | B_mergepilot | 12 | 9 | 5 | 57.14% | 70.59% | 63.16% |

- **decision accuracy**：A=50.00%（5/10），B=40.00%（4/10）。
- **正式结论**：在 N=10、同模型、每个 pair 单次运行的受控本地评测中，MergePilot-style 多角色编排相较单 Agent 将 precision 从 36.36% 提升至 57.14%，F1 从 48.00% 提升至 63.16%，recall 同为 70.59%。改善主要来自 FP 从 21 降至 9；代价是 token 增加 32.95%、API 请求增加 80%。B 的 decision accuracy 为 40%，低于 A 的 50%，风险处置校准仍需改进。
- **secret pattern 扫描**：synthetic_fixture_pattern_hits=2、real_credential_hits=0（命中字符串与 fixture 合成值完全相同；不输出明文）。
- **明确限制**：
  - 受控本地编排（controlled local orchestration），**Group B 不是真实 Gateway/controller/GitHub/HiClaw E2E**；
  - N=10 小样本，每 pair 单次运行，单模型 deepseek-v4-flash，synthetic fixtures；
  - 不支持真实 fix/verify/rollback 指标（已主动剔除，不用自报数）；
  - **不证明多角色提高 recall**（两组 recall 相同 70.59%）；
  - **C3 10/10 是独立的真实隔离栈证据**，不与 Benchmark 混为一项；
  - N≥10 最低目标已达成；N≥20 仍是后续目标。

**M4（核心 Skill 工程化）全部闭合**（M4-A/B/C/D/E 已发布）；M3（A/B/C）已全部闭合。B4 链遗留技术债 **MIG-B4-001**（forward-only，M7 空环境复现前解决）。**M4-F（AgentTeams 全链集成）已正式发布**（annotated tag `m4f-agentteams-demo-closed` → peeled D=`ee5158b`；`tests/m4f1/run_all.sh` 16/16 门禁、legacy 权威平台回归 6/6、六 Skill 全 SUCCEEDED、hiclaw_live=false；已 fast-forward 推送 origin/main）。**M5-0A HiClaw Live Candidate 已正式发布**（annotated tag `m5-0a-hiclaw-live-candidate-closed` → peeled delivery C=`c8b4c3d`；evidence commit E=`95ec0c7`；状态提交 R；已 fast-forward 推送 origin/main，无 force）；**M5-0 最小闭环尚未整体完成**（hiclaw_live=false），**M5-0B Agent Handoff 已正式发布**（annotated tag `m5-0b-agent-handoff-closed` → peeled delivery `da2b9c7`=C；evidence commit `9a6f465`=E；状态提交 R+R2；已 fast-forward 推送 origin/main，无 force；tag 目标为 C 不是 E/R/R2；详见第九节）；M5-0B 发布后已完成 Ubuntu-22.04 存储清理 + 存储防膨胀代码/部署。M5-0C C3 10/10 + D2B-1 17/17+6/6 已在 MergePilot-Test 验证（见第七.5节）。**正式 Benchmark N=10×2 已冻结**（见上）。下一主线：**OTel/SLS 可观测 → Benchmark N≥20 → 多仓库稳定性**。D2B-3 socket proxy 已完成（`hiclaw_live=true`，真实 v1.2.2 生产 live 64/64 PASS）。



## 七.5、M5-0C / D2B-1 / 存储防膨胀（2026-08-11 最新）

> HEAD = `4b053ef` = origin/main（ahead/behind=0/0）。

### C3 十轮稳定性（已验证）

- **10/10 PASS**，source_commit=`4b053ef`。
- MergePilot-Test 隔离测试栈（独立 dockerd + VHDX），**非生产 Ubuntu-22.04**。
- 传播修复（`4b053ef`）：`cleanup_gh` 删除分支后有界轮询 `list_branches` 确认已消失（`_verify_branches_gone`）；fail-closed 不假定已清理。
- evidence：`evidence/m5/0c/c3-10x.json`（10 runs、final_rc=0、negatives=15/15、residue=0）。
- **hiclaw_live=false**（隔离测试栈，非线上 HiClaw）。

### D2B-1 离线回归（已验证）

- **17/17 + 6/6**（M4-F run_all + legacy 6 平台），source_commit=`4b053ef`。
- evidence：`evidence/m5/0d/offline-regression.json`。

### 存储防膨胀 + guarded startup（候选/部分完成）

- **disk guard**（`tools/hiclab/disk_guard.py`）：host+guest 磁盘阈值门禁（guest≥100Gib / host≥150Gib），fail-closed；已部署于 `start-controller-container.sh` + `start-m5-0-candidate.sh`。
- **guarded startup**（`tools/hiclab/guarded_start.py` + `install_guarded.py`）：6 容器 restart=no + systemd service + capability preflight；无 marker 时 **block controller + manager、启动 0 容器**；生产部署验证 PASS（BLOCKED_UPSTREAM 日志、0 docker start）。
- **cleanup tooling**（`minio_cleanup.py` + `cleanup_run.py`）：mc-only dry-run-default；`--apply` fail-closed（无 authoritative probe）。
- **240 单元测试**（`tests/hiclab/`，host Python，无 WSL/Docker）。
- **诚实边界**：Manager auto-create 路径仍 BLOCKED_UPSTREAM（HiClaw controller 无 disable-auto-create 配置，审计确认）；socket proxy 策略已实现+测试（`harden_policy.py`）但 daemon 未部署。

### D2B-3 BLOCKED_UPSTREAM（确认）

- HiClaw `hiclaw-controller` 二进制（`hiclaw-embedded:latest`）经 `strings` / 环境变量 / CRD / 脚本全量审计：**无 disable-auto-create 配置**。
- Worker 创建是 controller 核心功能，无配置可禁用；controller 是唯一 Docker socket 访问者。
- D2B-3 需要经过审计的 Docker socket proxy。当前 capability preflight（不启动 controller）是唯一可行程序化防护。

## 八、M3 工程里程碑(实时;权威以标签为准)

- **B1–B5 已闭合**:`m3b-b1-closed` → `m3b-b5-closed`(见 [标签SHA映射](标签SHA映射.md))。
- **B4e(总 E2E)✅ `m3b-b4e-closed`**:review→fix→verify→discover→ticket→approve.sh→drain→MERGED(真 squash merge)+ lease 崩溃恢复 + UNKNOWN/EXECUTING 对账(绝不重 merge)+ Gateway 降级→恢复(circuit breaker)+ Matrix 非 L2 循环存活。**43/43 PASS,连续两次稳定,exit 0**。fixture 隔离(`nghqqa/MergePilot-e2e-fixture`,结束时 0 open PR / 仅 main)。delivery `63301e8` / tag object `26d0984` / peeled `63301e8`。见 [evidence/m3b-b4e/](../evidence/m3b-b4e/README.md)。
- **B5(负向证据)✅ `m3b-b5-closed`**:直连拒/list 过滤/跨角色拒/fixer 写约束/票据伪造与 args 篡改/过期与重复票拒/合法票单次执行/不可篡改审计。50/50 PASS，连续两次稳定，fixture 0 open PR、仅 main。
- **M3-C(状态感知失败处理 + 回滚)✅ `m3c-closed`**:child-run 模型(`parent_run_id`+`revert_run_id`,revert 复用正常 drain→MERGED);`POST_MERGE_VERIFY_FAILED` 真实入口(仅 verifier,校验 room/run/repo/pr/result_sha);changed-files/merge-parent/恢复内容由 GitHub 权威派生(修 `EmbeddedResource` 内容丢失);L2 前重验 main==bad merge + revert head==parent(冲突/不支持→HOLD)。**M3-C E2E 33/33×2**;B4 回归未破坏(B4c.1.6 5/5、B4e 43/43、B5 50/50);迁移非破坏幂等(零 `DROP TABLE`)。delivery `c8a2433` / tag object `ec5438b` / peeled `c8a2433`。见 [evidence/m3c/](../evidence/m3c/README.md)。
- **fresh-DB 闭合已验证(M3-C)**:全新空库完整链 `init → m3_state → m3b_policy → m3b_b4 → m3b_b4c → m3b_b4c1 → m3b_b4c1_1 → m3b_b4d1 → m3c_state` **9/9 rc=0**;Schema 断言 **19/19**(FK/UNIQUE/CHECK/函数);`m3c_state.sql` 独立重入 **rc=0**。措辞边界:只断言"fresh DB 单次铺设成功,M3-C 自身幂等",不声称全链完全幂等。
- **技术债 MIG-B4-001(B4 链,非 M3-C)**:`m3b_b4.sql` 经 B4d.1 把 `l2_approve` 演化为带 `DEFAULT NULL` 签名后,早版 `m3b_b4.sql` 无法 `CREATE OR REPLACE` 回退(全链二次重放 8/9,`m3b_b4.sql:193` XFAIL)。支持的生产路径=按版本 forward-only 单次应用。**M7 空环境最终复现前**须由正式 migration runner 记录版本/已应用集,或对齐 `l2_approve` 签名。
- **M3 完成后的下一主线曾为 M4；M4 现已全部闭合，当前主线见第九节。**

## 九、M4 进展(实时;权威以标签为准)

- **M4-A(公共 Skill Contract / runtime)✅ `m4a-runtime-closed`**:统一 Contract(Draft 2020-12)、公共 runtime(envelope/errors/redact/cli,fd+stream 隔离、1 MiB 限制、脱敏)、75 项确定性契约测试两轮稳定。peeled delivery `cd1a04a`。M4-B 期间对 `skills/common/runtime/cli.py` 做窄范围加固(新增 `_emit_finalized`,所有错误发射经 `_finalize` 脱敏;旧标签原位未动,M4-A 75/75×2 回归通过)。
- **M4-B(diff-parse + risk-classify)✅ `m4b-diff-risk-closed`**:已发布(peeled `07cc3da`)。两个框架中立核心 Skill;96 测试×2,M4-A 回归 75/75×2,M3 零 diff。
- **M4-C(sast-scan + test-runner)✅ `m4c-sast-test-closed`**：已发布（peeled `83442be`）。经多轮复审完成 raw-secret fail-closed、冻结硬上限、引擎不可降级、deadline、组件级路径安全、trusted-config、网络隔离、退出分类和 cleanup fail-closed。TestRunner v1 冻结为 container 8 MiB tmpfs 且 `artifacts=[]`，trusted-dev subprocess 可返回宿主 artifact digest；schema/core 双重强制。**87/87×2，M4-A 75/75 + M4-B 96/96 回归**。真实 WSL2 Docker 四场景 E2E（PASS/TIMEOUT/ERROR/TMPFS_QUOTA）通过，配额场景观测 `ENOSPC=28`，残留容器为 0。证据 `evidence/m4/m4c/`。
- **M4-D(PRLifecycle)✅ `m4d-pr-lifecycle-closed`**：设计冻结、实现、54/54×2 确定性测试、
  M4-A/B/C 全量回归和真实 Policy Gateway → github-mcp → GitHub fixture
  11 场景 E2E 均通过；open PR/测试分支/DB/runner residue 均为 0。peeled delivery `1f7e4b4`。
- **M4-E(CaseRetrieval)✅ `m4e-case-retrieval-closed`**：框架中立只读 RAG 检索 Skill，169 测试。Windows **166/3×2**、POSIX **158/11**（Docker `python:3.10-slim`）；POSIX 真实 tree-stub 3/3。真实 pgvector E2E 经一次性 Docker `pgvector/pgvector:pg16` fixture（`docker-fixture-e2e.json` schema v2），`all_passed=true`、Docker 残留 0/0、env-file fail-closed。deploy-owned repo scope/DSN/model/timeouts；数据库只读身份（`case_retrieval_reader` NOSUPERUSER/NOCREATEROLE/NOCREATEDB/NOREPLICATION/NOBYPASSRLS + `default_transaction_read_only=on`）+ SQL/core 双层跨 scope fail-closed；`untrusted` 恒为 true（历史案例始终为不透明数据，不授权 merge、不替代当次 SAST/TestRunner）。M4-A 75/75 + M4-B 96/96 + M4-C 87/87 + M4-D 54/54 回归。peeled delivery `621e3aa`。
- **M4-F（AgentTeams 全链集成）✅ 正式发布**（annotated tag `m4f-agentteams-demo-closed`，tag object `1a47683a87b16b6eed4b7a1778aafa97d60061c5`，peeled delivery D=`ee5158b`；evidence commit G=`08770be`；已 fast-forward 推送 origin/main，无 force）：在 M4-F1 数据面之上闭合真实协议 E2E——`controller.process_event(M4F_RUN)` → Policy Gateway → fake GitHub MCP（SSE）→ 六 Skill DAG → PRLifecycle。`tests/m4f1/run_all.sh` **16/16 门禁全过**（schema/JCS/SD API/并发/worker/完成/purge/runtime/hygiene/whitespace/Demo/AgentTeams + release evidence 反例 + legacy 功能回归），`final_rc=0`、residue 0/0/0。**legacy 权威平台回归 6/6 MATCH**：M4-A 75、M4-B 96、M4-C 87、M4-D 54（win32 `m4a-venv`）、M4-E win 166/3、M4-E posix 158/11（container `--init`，tini 进程树 reap）。六 Skill 全 SUCCEEDED + 全 schema 验证：test-runner **PASS**、case-retrieval **真实 pgvector** complete=true、pr-lifecycle **CREATED**（真 Gateway as fixer）；secret_leaks=0、**hiclaw_live=false**（上游为协议 fixture，非线上 HiClaw）。delivery_digest `b595e561…`（53 文件）三处一致；evidence：`evidence/m4/m4f/{agentteams-e2e.json,agentteams-demo-summary.json,verification.txt,legacy-functional-regression.txt,full-chain-e2e.json}`。
- **M4-F producer 并发 hotfix ✅ 正式发布**（annotated tag `m4f-producer-concurrency-hotfix1-closed` → peeled C2=`0a1714a`；状态提交 R；已 fast-forward 推送 origin/main，无 force）：发布后真 PG16 双连接竞态捕获 `enqueue_snapshot_job`/`enqueue_skill_job` 的 `ON CONFLICT (job_id)` 在双唯一约束下偶发泄漏 SQLSTATE 23505；hotfix 改无 target `ON CONFLICT DO NOTHING`（fresh `m4f1_state.sql` + additive `m4f1_hotfix_1.sql`）。提交链 C=`b1da394`→E→C2=`0a1714a`→E2=`4215779`→R；**17/17 门禁 gates_total=17**、20/20 并发 0 次 23505、fresh/升级两路径 buggy=2→fixed=2、legacy 6/6、delivery_digest `04524c63…`（56 文件）四处一致、secret=0、residue 0/0/0、**hiclaw_live=false**。证据 `evidence/m4/m4f/` + `evidence/m4/m4f-hotfix1/`。
- **M5-0A HiClaw Live Candidate ✅ 正式发布**（annotated tag `m5-0a-hiclaw-live-candidate-closed` → peeled delivery C=`c8b4c3d`；evidence commit E=`95ec0c7`；状态提交 R；已 fast-forward 推送 origin/main，无 force）。Candidate Controller 经真实 Matrix `/sync` ingress——隔离 throwaway **mini homeserver**（`tests/m5_0/fixtures/mini_matrix_hs.py`，标准 Matrix CS 端点 + 真实 event_id/分页，**非线上 HiClaw**，不经手真实 Matrix/外部凭据）→ claim（`run_id` 与 `M4F_PENDING` 同一 UPDATE 原子写入，`_drain_m4f_claim_sql_prefix` 参数化 `LIKE %s` 前缀隔离，生产 `M4F_ENABLED=0` 不 claim）→ 六 Skill DAG enqueue → `PROCESSED`。严格解析器（`m5_parse_m4f_run`/`m5_parse_handoff`/`m5_parse_verify`）替换子串匹配；前缀 charset/SQL 通配/父子重叠 fail-closed；Candidate 独立 `GATEWAY_ROLE=m5coordinator` 只读身份（不持生产 `COORDINATOR_TOKEN`，gateway.py v2.5 provenance 极窄勘误 `{coordinator,m5coordinator}` 仍限 `pull_request_read`+`method=get`）；session-level advisory lock singleton（`application_name` 精确标识；第二 Candidate denied、lock backend 断开 → exit 1）；`controller_offsets` 独立 consumer。**门禁**：`tests/m5_0` 单元 **68 passed**、Candidate integration **13/13 PASS**、`tests/m4f1/run_all.sh` **17/17 PASS `final_rc=0`**、legacy 权威平台回归 **6/6 MATCH**（M4-A 75 / M4-B 96 / M4-C 87 / M4-D 54 / M4-E-win 166/3 / M4-E-posix 158/11）、delivery_digest `b8a72b6d…`（**56 文件**，recomputed==stored）、**secret_leaks=0**、**residue 0/0/0**、**hiclaw_live=false**。证据 `evidence/m5/0a/`（README + regression + integration + unit；`evidence/m4/m4f` 历史 M4-F evidence 零 diff）。**M5-0B 已正式发布（见下条）；M5-0C C3 10/10 + D2B-1 17/17+6/6 已验证（见第七.5节）。hiclab_live=false。**
- **M5-0B Agent Handoff（Reviewer/Fixer/Verifier reconciliation 闭环）✅ 已正式发布**。发布提交链 `Base(124831b) → I(9a4bff9, test: isolate Docker runners) → C(da2b9c7, feat: add M5-0B Agent handoff workflow) → E(9a6f465, docs: record M5-0B Agent handoff release evidence) → R(5572ec2, docs: mark M5-0B released) → R2(docs: finalize M5-0B release status)` 线性、无 merge/rewrite、reflog 仅正常 commit；`evidence/m5/0b/` 共 **9 文件**（README、release-summary.json、handoff-e2e、concurrency、delivery-digest、isolation、unit-tests、m4f-regression-verification、m4f-regression-legacy）。**发布验证**：Docker/WSL 隔离 **11/11 PASS**（guard fail-closed rc=2 on wrong distro、生产容器测试 daemon 不可见、canary residue=0）；M5-0A 回归 candidate **13/13** + lifecycle **5/5** + advisory lock **4/4**；M5-0B handoff integration **14/14 PASS**、concurrency+negative **13/13 PASS**；`tests/m5_0/test_m5_0b_delivery_digest.py` **71 passed**；`tests/m5_0` committed-C 最终 **184 passed / 0 skipped / 0 failed**（`test_release_evidence.py` 9/9 为 run_all 容器门，Windows host GBK/python3 限制不计为产品失败）；`tests/m4f1/run_all_test.sh` **17/17 `final_rc=0` ALL GATES PASSED**（经官方 isolated wrapper，`evidence/m4/m4f` repo 外备份后逐字节恢复）；legacy 权威平台回归 **6/6 MATCH**（M4-A 75 / M4-B 96 / M4-C 87 / M4-D 54 / M4-E-win 166/3 / M4-E-posix 158/11）；check_hygiene `rc=0 scan_hits=0`；**secret_leaks=0**、**residue 0/0/0**、Ubuntu-22.04 BEFORE/AFTER 均 Stopped、生产 Docker 未访问。**Delivery**：`delivery_digest 918691c4364e851117b856339d326b9ea06e6b9e435b6dde03c256509dee9d06`（**92 文件**，`missing=0 unexpected=0`，两遍稳定；evidence head=`da2b9c7`=C，evidence 不进 surface，C/E digest 完全一致）；M4-F digest `7439b54883aafc92bf95bd414113ed2cf2fabf3e62406c367bdf077d4776a0ee`（57 文件，M5-0B 严格子集 M4-F\M5-0B=0；`tests/m4f1/delivery_digest.py` 自 Base 未修改）。**业务语义**：`reconcile_m5_skill_to_review` / `reconcile_m5_handoffs` / `advance_m5_review_run` 闭合 skill→review→fix→verify DAG——精确一 review stage + 一 reviewer dispatch、COMPLETED review → fix stage + fixer dispatch、fixer → verify stage + verifier dispatch；精确 run/revision/job/invocation provenance（schema-validated）；`handoff_watcher` 排除 `m5live-*` 前缀（生产 HiClaw live run 不被测试 watcher 触碰）；Verifier `VERDICT=PASS → HOLD/m5_verify_passed`（**不是 COMPLETED**，等待外部 approve.sh drain）；`VERDICT=BLOCKED/FAIL → HOLD/m5_verify_failed`；replay 幂等、wrong-sender handoff fail-closed、payload 冲突 rollback（既有行不动）、binding 负向四类零 advance。**诚实边界**：**hiclaw_live=false**；本轮使用隔离 mini Matrix homeserver + MergePilot-Test 测试 daemon（独立 dockerd+VHDX，六生产容器不可见）；**未接入真实生产 HiClaw**、**未访问生产 Docker**、Ubuntu-22.04 全程 Stopped；**M5-0 最小闭环尚未整体完成**。**发布信息**：已正式发布——annotated tag `m5-0b-agent-handoff-closed`（peeled delivery `da2b9c7`=C，**tag 目标为 C，不是 E/R/R2**）；evidence commit `9a6f465`=E；状态提交 `5572ec2`=R + R2；main 已 fast-forward 推送 origin/main，无 force；evidence head=`da2b9c7`=C；digest 保持 `918691c4364e851117b856339d326b9ea06e6b9e435b6dde03c256509dee9d06`/92（C/E/R/R2 完全一致，evidence 不进 surface）；**hiclaw_live=false**、未接入真实生产 HiClaw、M5-0 最小闭环尚未整体完成。
 → M5-0B 正式发布后先精确清理/压缩原 Ubuntu-22.04 生产 WSL → M5-0C 真实 Gateway/GitHub/HiClaw 链路 → M5-0D `hiclaw_live=true` 与稳定性证据 → M5 Benchmark 与量化价值证明 → M6 完整 OTel/SLS、生产可靠性和多仓库运行 → M7 最终提交材料与干净环境复现。

## 九、M6-RAG 与 M7-P2 RAG Benchmark（2026-08-12）

### M6-RAG CaseRetrieval 垂直闭环 ✅ `m6-rag-pgvector-closed`

Reviewer（sast_scan）与 Fixer（pr_lifecycle）通过 `CaseRetrievalBridge → core.run → PgVectorAdapter → pgvector` 检索历史案例。检索结果作为标准 `evidence[]`（`kind: "rag_advisory"`）附加到 handler 返回值——`adopted=false`、`untrusted=true`、**不跳过 Verifier**（core.scan/core.run 在 RAG 之后始终执行）。

pgvector 隔离验证 42/42 PASS（一次性容器、非特权 ragreader、SQLSTATE 57014 timeout、post-timeout 恢复、真实 Skill CLI）。M4-E delivery digest 恢复为 `e2c283b3…`。OTel rag.query/result/fallback span 与 W3C traceparent 传播。

**诚实边界**：`core.scan()` 和 `core.run()` **不消费** RAG 检索结果——RAG 是 advisory evidence only。因此 workflow utility（Reviewer/Fixer 决策准确率提升）**不可测**（`NOT_MEASURABLE_WITH_CURRENT_RUNTIME`）。

### M7-P2 RAG Benchmark — Development Calibration（rag-bench-v2）

- **dataset_version**: `rag-bench-v2`；N=29 独立 case；20 知识库 case。
- **cohorts**: positive_retrieval=19、abstention=5、fault_injection=5。
- **benchmark_phase**: `DEVELOPMENT_CALIBRATION`；`quality_gate_pass=null`（**非确认性，不作为质量结论**）。
- **adapter**: 确定性离线 `TokenOverlapAdapter`（token-Jaccard），**非真实 pgvector embedding**。
- **abstention**: 4/5=80%（1 false positive，bm-002 clean 查询弱匹配 kb-clean-01）。
- **determinism**: normalized_semantic_digest 两次运行完全一致。
- evidence: `evidence/m7/benchmark/rag-n20-offline.json`。

### M7-P2 RAG Benchmark — Confirmatory Held-out（rag-bench-v3-heldout）

- **dataset_version**: `rag-bench-v3-heldout`；N=25 独立 held-out case；20 知识库 case。
- 与 v2 **完全分离**（0 case_id/sample_id/query 重叠；独立 seed=99）。
- **cohorts**: positive_retrieval=15、abstention=5、fault_injection=5。
- **total_arm_executions**: 50（≥40）。
- **benchmark_phase**: `CONFIRMATORY_HELDOUT`；`quality_gate_status=PRE_REGISTERED`。
- **预注册质量门控**: 16 项阈值在执行前冻结，**16/16 PASS**。
  - hit@1=1.0（≥0.70）、hit@3=1.0（≥0.80）、MRR=1.0（≥0.75）
  - top1 category/severity match=1.0（≥0.70）
  - abstention accuracy=0.60（≥0.60）、scope leak=0
  - fault fallback/timeout/malformed fail-closed=1.0
  - deterministic replay=true、gold/secret/residue=0
- **confirmatory_all_ok=true**（execution + safety + quality 三层门控全通过）。
- evidence: `evidence/m7/benchmark/rag-n20-confirmatory.json`；SHA256 `36edc664…`。
- runner commit C2=`efba869`；evidence commit E2=`60d8d29`。

### 诚实边界

- `runtime_consumes_rag_context=false` — core.scan/core.run 不读 RAG。
- `workflow_utility_status=NOT_MEASURABLE_WITH_CURRENT_RUNTIME`。
- `verifier_execution_status=NOT_MEASURED` — Verifier 未在该 Benchmark 中实际运行。
- `database_residue_status=NOT_APPLICABLE` — 使用 Fake/TokenOverlap adapter。
- Benchmark 使用确定性离线 adapter，**不外推为真实 pgvector/embedding 或生产多仓库效果**。
- **不声称** RAG 提升 Reviewer/Fixer 准确率或最终修复质量。
- **M7 尚未整体完成**；下一阶段为 M7-P3 Demo Console 与干净环境复现。

### M7 正式关闭 (2026-08-13)

- **关闭 tag**: `m7-closed` → peeled `175541a`。
- **完成范围**: M7-P2 / P3 / P4 全部 merged（RAG Benchmark、Portfolio Profile、Demo Console REPLAY）。
- **DEFERRED**: Demo video **DEFERRED**（非阻塞，后续补录，不影响关闭）。
- **M8 未定义**（no scope / no schedule）；下一里程碑待规划。
