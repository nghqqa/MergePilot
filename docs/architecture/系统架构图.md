# MergePilot 全系统架构

## 概览

```mermaid
graph TB
    subgraph OPS["操作员（人工）"]
        OP["👤 操作员<br/>安全门决策 · 审批 · 拒绝"]
    end

    subgraph GH["GitHub（外部）"]
        REPO["nghqqa/fastapi-boilerplate-demo<br/>PR #2 · PR #3<br/>review→fix→verify，零写入"]
    end

    subgraph ELEMISO["elemiso 隔离栈（Docker Desktop · elemiso-net）"]
        direction TB

        subgraph CTRL["elemiso-ctrl（AgentTeams v1.2.3 嵌入式控制器）"]
            direction LR
            API["Controller API<br/>:8090"]
            MATRIX["Matrix Tuwunel<br/>:6167"]
            MINIO["MinIO<br/>:9000"]
            GATEWAY["Higress AI Gateway<br/>:8080 → deepseek-chat"]
            CONSOLE["Element Web<br/>:8088"]
        end

        subgraph WORKERS["CoPaw Workers（223ddc2-build1）"]
            LEADER["👑 Leader<br/>编排 · 委派 · 验收<br/>projectflow / taskflow"]
            REVIEWER["🔍 Reviewer<br/>独立审查<br/>自写 PoC · 自主定级"]
            FIXER["🔧 Fixer<br/>最小修复<br/>本地补丁 + SHA256"]
            VERIFIER["✅ Verifier<br/>干净 clone 验证<br/>不信任 Fixer"]
        end

        LEADER -->|"@mention 委派"| REVIEWER
        REVIEWER -->|"提交 result.md"| LEADER
        LEADER -->|"@mention 委派"| FIXER
        FIXER -->|"提交 diff + result"| LEADER
        LEADER -->|"@mention 委派"| VERIFIER
        VERIFIER -->|"VERDICT=VERIFIED"| LEADER

        REVIEWER & FIXER & VERIFIER & LEADER -.->|"LLM 调用<br/>deepseek-chat"| GATEWAY
        REVIEWER & FIXER & VERIFIER & LEADER -.->|"消息收发"| MATRIX
        REVIEWER & FIXER & VERIFIER & LEADER -.->|"工件读写"| MINIO
    end

    OP -->|"批准 / 拒绝<br/>(人工门决策)"| LEADER
    LEADER -->|"发现摘要<br/>请求门决策"| OP

    ELEMISO -.->|"克隆 (只读)<br/>head SHA 校验"| REPO

    style OP fill="#FFF3E0",stroke="#E65100"
    style REPO fill="#E3F2FD",stroke="#1565C0"
    style LEADER fill="#FFF3E0",stroke="#E65100"
    style GATEWAY fill="#E8F5E9",stroke="#2E7D32"
    style MATRIX fill="#E3F2FD",stroke="#1565C0"
```

## R2 主案例运行流程

```mermaid
sequenceDiagram
    participant OP as 操作员(人工)
    participant L as Leader(CoPaw)
    participant R as Reviewer(CoPaw)
    participant F as Fixer(CoPaw)
    participant V as Verifier(CoPaw)
    participant GH as GitHub(只读)

    Note over OP,GH: run-elem-pr2r2-20260916-01 · 全程守候 16 分钟 · 88 次 LLM 调用

    OP->>L: kickoff (console channel)
    L->>L: projectflow(ready_nodes) → pr2r2-review-1
    L->>R: taskflow(delegate_task) @reviewer<br/>事件 $sWmvb_KJ…

    rect rgb(255, 243, 224)
        Note over R: ② 独立审查（非预设 SPEC）
        R->>R: clone + checkout 1dedf5e1
        R->>R: 自写 PoC：../ 逃逸基目录读 /etc/hostname
        R-->>L: FINDING_CONFIRMED / HIGH / CWE-22<br/>HUMAN_VERIFICATION_REQUIRED: YES
    end

    rect rgb(255, 248, 225)
        Note over OP,L: ③ 人工安全门（HUMAN VERIFICATION REQUIRED）
        L->>OP: 停门报告：尚未委派 fix，等待决策
        OP->>L: [HUMAN GATE - APPROVED]<br/>DM $0NQWnmvF…
    end

    L->>L: taskflow(delegate_task) pr2r2-fix-1
    L->>F: @fixer 委派

    rect rgb(232, 245, 233)
        Note over F: ④ 最小修复
        F->>F: clone + checkout + 修改单文件 +13/−7
        F->>F: realpath + commonpath 边界校验
        F-->>L: FIX_APPLIED · sha256 674356fc…
    end

    L->>V: 委派 pr2r2-verify-1
    rect rgb(232, 245, 233)
        Note over V: ⑤ 独立验证（干净 clone）
        V->>V: 全新 clone + apply patch
        V->>V: 修复前 3 组越权 200+泄露
        V->>V: 修复后 400×3 / 合法 200 / 缺失 404
        V-->>L: VERDICT=VERIFIED 首次通过
    end

    L->>OP: ⑥ 终报：项目 completed<br/>PR OPEN · 零 GitHub 写入
```

## 三轮真实运行对照

```mermaid
timeline
    title 2026-09-16 三轮真实运行时间线
    R1 首跑 : kickoff → 审查 HIGH → 门批准 → 修复 → 验证 PASS<br/>问题：门后派发链不干净<br/>处置：独立审计指出并修正
    PR3 拒绝 : Reviewer 确认 RCE → 操作员拒绝<br/>fix/verify 零派发 · 项目 blocked
    R2 干净复跑 : 非预设 SPEC → 审查 HIGH → 现场批准<br/>→ 修复 → 独立验证 VERIFIED PASS<br/>16 分钟 · 与 R1 补丁逐字节一致
```

## 安全边界

```mermaid
graph LR
    subgraph SYSTEM["MergePilot 系统边界"]
        A["Agent 审查"]
        B["Agent 修复"]
        C["Agent 验证"]
    end

    subgraph HUMAN["人工决策（系统外）"]
        D["操作员"]
    end

    subgraph GH2["GitHub"]
        E["PR #2 · OPEN<br/>PR #3 · OPEN"]
    end

    A -->|"发现 HIGH"| B2["🚫 人工门（强制停等）"]
    B2 -->|"批准"| B
    B2 -->|"拒绝"| F2["❌ 永久 BLOCKED"]
    B --> C
    C -->|"VERIFIED PASS"| E2["✅ 交付补丁"]
    E2 -.->|"维护者决策<br/>(不在系统内)"| E
    style B2 fill="#FFF3E0",stroke="#E65100"
    style F2 fill="#FFEBEE",stroke="#C0392B"
```
