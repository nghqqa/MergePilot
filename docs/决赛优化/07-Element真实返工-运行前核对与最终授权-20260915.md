# 07 · Element 真实返工 · 运行前最终核对与授权清单（2026-09-15，第五轮）

> 结论：**READY_FOR_AUTHORIZATION**（第一层 AgentTeams 原生返工，Manager 为唯一状态权威与唯一派发者）。本文所有事实均为本轮只读核验（docker inspect 仅取非敏感字段；未启动任何容器、未发消息、未调用模型、未写测试仓）。
> 本文修正并取代 `06` 中与实测不符的两处假设（房间拓扑、角色来源）。

## 1. 基线状态（实测）

| 项 | 值 |
|---|---|
| 分支/HEAD | `feat/finals-organizer-response` = 远端 = **`f0424c6`**（领先 origin/main 19 提交，工作树干净） |
| 草稿 PR | https://github.com/nghqqa/MergePilot/pull/230（draft，CI selftest node18/20/22 通过） |
| 测试摘要 | 3.9 组合 **1822/0/15**；m4b–m7 里程碑对照（vs origin/main 同机）：分支 **989/43** vs main 988/44，43 个失败同名同数=本机环境既有（m5_0c bash 夹具 ×38、m4c ×3、m4e ×1、m4f1 ×1）；分支已修复 main 也有的 m4b 夹具 CRLF 问题与自身 4 个清单回归（m5_0 184/184、m4b 96/96） |
| 未完成（明确列出） | Element 真实交接（本文）、真实 LLM 模型轴、PolarDB Agentic Database、PR 合并/Release/材料上传——均未执行 |

## 2. 实际拓扑（实测，修正旧文档假设）

| 组件 | 容器 / 镜像 | 实测关键值 |
|---|---|---|
| 嵌入控制器（Matrix+MinIO+API） | `p14h2-wd-ctrl` / `agentteams/agentteams-embedded:223ddc2`（2026-08-28 构建） | 端口 6167/8090/9000；挂 docker.sock |
| Manager（AI 协调者，**唯一派发者与状态权威**） | `agentteams-manager` / `agentteams/manager:223ddc2` | 端口 18888；provider=qwen、model=**deepseek-chat**、AI 网关 `http://p14h2-wd-controller:8080`（另有 `AGENTTEAMS_AI_GATEWAY_ADMIN_URL`，值未读） |
| CoPaw Worker ×4 | `agentteams-worker-p14h2-copaw-worker-{reviewer,fixer,verifier,manager}` / `copaw-worker:223ddc2-build1` | **每 Worker 独立房间**：reviewer `!fai6WChnDcFHxNnuK2`、fixer `!k58dEV9n3rJOk90LCI`、verifier `!VYuRwxSa8aU2YvNdKR`、manager-worker `!X3AhSfiuqGOw9zz2DO`（域均为 `p14h2-wd-matrix:6167`）；`AGENTTEAMS_WORKER_ROLE=standalone` ×4 —— **角色由 Manager 派发任务时赋予，不由容器名/环境固定**（旧文档假设已修正） |
| Element Web | `p14h2-wd-element-web` / `vectorim/element-web:latest` | `127.0.0.1:18088` |
| 启动前资源状态 | 全部 11 个容器 **Exited**、0 个运行中；快照存 `D:/mp-finals-tmp/prestart-containers-20260915.txt` | 网络 `p14h2-wd-net`、`p14h2-copaw-net` 存在 |

## 3. 权威链路（核对结论）

- **状态权威 = AgentTeams Manager 的 project/task 状态**（CoPaw TeamHarness 语义，存于 ctrl 内 MinIO `manager/state.json`；状态含 `delegated/in_progress/submitted` → plan node `completed/revision/blocked`）。链路：Verifier Worker 提交 `TaskResult(FAIL + 测试报告)` → **Manager 验收** → 将 Fixer 任务置 `revision` 重派，或 `blocked` 升级人工。依据：`p12/agentteams-v123/docs/design/teamharness/project-task-runtime-design.md` 状态表 + `docs/usage/project-workflow-api.md`（`revision`/`record_loop_iteration`/history API）。
- **不由人工消息或预设脚本替代**：Verifier 必须在干净 checkout 上真实运行 `python -m unittest tests.test_refund_acceptance` 并以退出码出 VERDICT；重派决定由 Manager 基于 TaskResult 作出，操作员不代答。
- **MergePilot PG 控制器与该栈未接通**（结构性事实）：若要求 PG 权威，需 A5-E 另起隔离栈并仅作**只读镜像记录**（绝不派发，避免与 Manager 争用同一任务）。本轮默认**不启用**。
- **不部署本分支代码到现有团队**：容器为镜像固定版本（223ddc2），本案例不需要其中的 m9/网关改动；案例文件经测试仓分支交付。如启用 E2 观察者，用本分支代码起**新增**带标签容器，恢复方式=删除该批新容器。

## 4. GitHub 写入范围（实测核对）

- 测试仓 `nghqqa/fastapi-boilerplate-demo`：现存分支 `main`、`demo/high-risk-human-gate`、`demo/high-risk-human-reject`、`mergepilot-demo/schema-migration-risk`；**3 个历史 PR 全部 OPEN（未合并）**。
- 目标分支改为 **`mergepilot-demo/rework-refund-20260915`**（实测不存在=404）：沿用仓内唯一由我们创建过的前缀 `mergepilot-demo/*`，不新造 `mergepilot/*` 前缀（不放宽/不猜测 policy）。
- 禁止：merge、写 main、修改冻结验收测试 `tests/test_refund_acceptance.py`（Fixer 指令中明示；违规即 HOLD）。Worker 侧分支名 allowlist 若存在只能启动后确认 → 列入阶段 0 待验证项，不通过则 typed failure，不改 policy 迁就。

## 5. 预算（四上限，最先达到者停止新调用）

| 维度 | 上限 | 统计/执行方式 |
|---|---|---|
| 模型 | deepseek-chat（qwen 网关路由，与历史一致） | 启动后 `agt llm-preflight` 计为**请求 #1** |
| 总 token | **≤ 300,000**（历史全流程 51,890，余量 ≈5.8×） | 阶段 0 读网关/日志用量基线，运行中每 ≤5 分钟采样**全部 Manager+4 Worker 合计**；来源待验证（见下） |
| 请求数 | **≤ 60** | 同上合计 |
| 墙钟 | **≤ 60 分钟**（自 kickoff 起） | 操作员计时 |
| 费用 | **≤ ¥10**（deepseek-chat 公开价约 ¥1–4/百万 token 量级；以账单为准） | 由 token 数乘价估算 + 事后账单核对 |
| 达限动作 | 立即停止新调用：`docker stop agentteams-manager`（派发停止，状态保留、可恢复；如个别 Worker 正在调用一并 stop） | 状态与已发生消息全部保留 |

诚实声明：嵌入版网关（ctrl:8080）是否有用量端点/硬预算**停机状态无法只读确认** → 阶段 0 待验证项；若无端点，则用 Manager/Worker 日志 token 字段合计（历史 PHASE 运行曾以 tokens/requests 记账，来源存在），仍不可靠时按最保守值（日志缺失即视为超限）提前停止——**不会"无法统计就继续跑"**。

## 6. 阶段 0（授权后、任何 kickoff 之前，全部只读/一次性小成本）

1. 按 A1 顺序 `docker start`；等 6167/8090/9000 健康；记录启动后快照（对照 §2 快照，差异即异常）。
2. 只读确认四项（未通过 → 中止并 typed failure，**不 kickoff**）：① Manager state 路径可读（MinIO `manager/state.json` 或 `agt`/workflow API）；② 网关用量来源可用（端点或日志 token 字段）；③ 每房间消息可导出（admin 登录于 ctrl 内 `127.0.0.1:6167`，token 不落盘到证据）；④ `agt llm-preflight`（1 次小调用，计入请求 #1）确认 deepseek-chat 路由 200。
3. 建 GitHub 分支 `mergepilot-demo/rework-refund-20260915` + OPEN PR（内容=任务说明+验收测试+缺陷基线）；记录 PR 号与 head_sha₀。
4. 在 admin↔Manager DM 发 kickoff（run_id=`run-elem-20260915-01`）：任务、验收 4 条测试名、**分支唯一且禁止改验收测试**、fix/verify 各 ≤3 次、第 3 次 FAIL → `blocked` 升级。

## 7. 运行与证据（要点，全文见 `06`）

自然案例优先：不保证首修失败，first-pass PASS 如实记录；故障注入（Scenario B）**默认不启用**，如需另行单独确认注入点（幂等测试 #3 运行前置入一行冲突数据）与撤销方式。证据目录 `evidence/FINALS-ELEMENT-REWORK-20260915/`（新目录）：每条消息（含返工与升级）Matrix event_id、每次 head_sha 与 diff、每轮测试输出、Manager state.json/`agt` 导出（revision/blocked 转移前后）、token/请求采样表、Element 操作员截图、SHA256SUMS。Element 仅作人可见交接窗口，权威以 Manager 状态为准。结束：只 `docker stop` 本次启动的容器（对照快照，原本 Exited 的保持 Exited）；卷/消息历史/分支与 PR 保留。

## 8. 最终授权清单（一次确认；逐项回复亦可）

| # | 授权 | 内容 |
|---|---|---|
| **A** | 启动现有团队（仅此 7 容器，按序）：`p14h2-wd-ctrl` → `agentteams-manager` → 4 × `agentteams-worker-p14h2-copaw-worker-*` → `p14h2-wd-element-web`；结束仅 stop 本次启动者 | 不启动 openclaw worker；不动其他容器/镜像/卷/网络 |
| **B** | Matrix 消息范围：admin↔Manager DM（kickoff/澄清/收尾）+ 四个 Worker 房间（Worker 角色回帖、**返工 revision 通知、blocked 人工升级**、验收结论）+ 阶段 0 的房间消息导出；Element 房间 ID 即 §2 四个 | 不新建房间；不发往任何其他目的地 |
| **C** | 付费模型调用：deepseek-chat，总 token ≤300k、请求 ≤60（含 llm-preflight #1）、墙钟 ≤60min、费用 ≤¥10；达限即 §5 停止动作 | 不换模型/密钥；不调用其他付费服务 |
| **D** | GitHub 写入（仅测试仓 `nghqqa/fastapi-boilerplate-demo`）：新建分支 `mergepilot-demo/rework-refund-20260915` + 1 个 OPEN PR；Fixer 仅向该分支提交 | 不 merge、不写 main、不改验收测试、不动 3 个历史 PR |
| **E** | 清理范围：结束后 stop 本次启动容器（快照对照）；分支/PR 保留；证据新目录。**E2（可选，默认关）**：本分支代码起的隔离栈作只读 PG 镜像（新标签容器，不派发） | 不删除任何既有卷/消息历史/容器 |

**回复格式**："A–E 全部批准（E2 开/关）"或逐项（如"A、B、C 批准；D 批准；E 批准，E2 关；Scenario B 不启用"）。未批准前不启动、不调用、不发消息、不写仓。

---

# 修订 v2（2026-09-15 第五轮，应项目方五点要求；只读核对，未启动/未调用/未发消息/未写仓）

## v2.1 等级分列（修正宣称口径）

- 本轮性质 = **AgentTeams 原生真实协作验证**：状态权威为 **AgentTeams Manager**（镜像 `agentteams/manager:223ddc2`，源 commit `223ddc2b`，与运行镜像标签 `mp.upstream.commit=223ddc2b` 对应）；证据目录 `FINALS-ELEMENT-REWORK-20260915`，标签 **REAL_EXECUTED-AgentTeams**。
- **不**计为"MergePilot Controller/PG 已接通真实 Worker"：MergePilot PG 返工机制（`rework_loop_harness` + `FINALS-REWORK-LOOP-20260914`）是**另一组机制验证**（CONTROL_PLANE_MECHANISM，组件：本分支 controller.py@`f0424c6` + 独立 PG）。两组组件版本与证据**分列**，不合并宣称集成已通过。E2（PG 只读镜像）默认关闭不变。
- 版本分列记录：运行容器全部为 2026-08-28/29 构建的 `223ddc2` 系镜像（embedded/manager/copaw-worker:223ddc2-build1/element-web:latest）；本分支代码（`f0424c6`）**不部署**进任何现有容器。

## v2.2 分支前缀的实际生效 policy（实测源码结论）

- 对 AgentTeams v1.2.3 源码（= 运行镜像 commit）全文检索：**平台层不存在任何分支前缀 allowlist / branch policy 强制**（copaw、agentscope_runtime 运行时、manager 侧均无）。Worker 的 Git/GitHub 行为由**任务指令**与 **ctrl 内 `AGENTTEAMS_GITHUB_TOKEN` 的仓库级 scope** 约束。
- 因此分支名不构成权限问题，也**不以历史命名替代验证**：`mergepilot-demo/rework-refund-20260915` 仅作为任务指令中的唯一可写分支写入 kickoff；真正边界 = D 授权范围 + 验收测试冻结 + 阶段 0 后 PR 提交范围审计。
- Token scope 在不读取其值的前提下**无法只读验证** → 授权时请你在 GitHub 后台确认该 token 仅限 `nghqqa/fastapi-boilerplate-demo`；否则以"指令约束 + 事后提交审计"为边界（如实标注，不冒充已验证）。

## v2.3 启动恢复行为（源码 + 历史实证）与隔离方案

- 源码：copaw worker 启动 = MinIO 文件 mirror（`sync.py`：仅拉取 `openclaw.json/mcporter.json/skills/`），**无 LLM 调用**；设计文档明确 project/task 上下文恢复发生在"channel 或后续事件唤醒时"——无事件不触发。历史实证（PHASE13.1 `lifecycle-restart-results.json`）：`docker restart` 与冷启 stop/start 后 Team/Workers/Matrix 状态 **IDENTICAL**，无自发任务执行。
- 残余未知：p14h2 的 Manager `state.json` 当前内容在卷内，**不启动不可读** → 阶段 0 强制项：读取 `state.json`/workflow API，要求**无非终态任务**（无 active_tasks 或全部 completed/revision/blocked/cancelled）；若有 → **中止并报告，不清空、不修改任何历史队列**。新案例用全新 `run_id`/project，与历史任务零共享；历史任务所在消息域（hiclaw homeserver）与 p14h2 房间不同源，不进入本次预算。

## v2.4 预算控制（诚实口径 + 停止开关）

- 实测平台参数（镜像内常量，未设任何 `COPAW_LLM_*` 覆盖）：每 worker 并发上限 **10**（信号量）、QPM **600**（滑动窗，防 429 用）、重试 3（退避 1–10s）、**每请求 `max_tokens` 默认无上限**（oai schema `None`）；跨进程**无聚合限制器**，运行时**无预算端点**。
- 结论：**本栈不存在原生硬预算控制**。v1 文案中"停 Manager"不称硬预算（仍有 4 个 worker 可被消息触发）。修订后的控制设计：
  1. **计量**（不称硬控）：阶段 0 确定用量源——优先网关 admin API（`AGENTTEAMS_AI_GATEWAY_ADMIN_URL`，单一入口天然聚合 Manager+4Worker），否则 worker/manager 日志 usage 字段逐进程合计；采样周期 ≤5 分钟，**在途请求按 +1 预计入数**。
  2. **硬控（若网关支持）**：阶段 0 在网关配置 token/请求配额——这是唯一能"阻止所有调用者"的原生机制；可用性列入阶段 0，不预设。
  3. **停止开关（无条件可用）**：软阈值 80%（**240k token / 48 请求 / 48 分钟**）触发 `docker stop agentteams-manager agentteams-worker-p14h2-copaw-worker-reviewer …fixer …verifier …manager`——**停止全部 5 个调用者**，新调用不可能发出；在途流最多完成当前请求（受 `AGENTTEAMS_AI_STREAM_IDLE_TIMEOUT` 约束），计入余量（20% token + 12 请求余量即为此保留）；状态与消息全部保留、可恢复。
  4. 并发余量：最坏 5 调用者 × ≤10 并发；本案例任务串行（review→fix→verify），实际并发 ≈1–2；请求计数含在途预记，杜绝"达标瞬间又发出 N 个"。

## v2.5 推送收尾

本地 = 远端 = **`4564b6c`**（`git ls-remote` 复核 + gh API 双确认），PR #230 head=`4564b6c`、draft、OPEN；后台重试循环已全部结束，无遗留任务。本修订以有界重试（≤6 次）推送，失败则明示，不无限重试。

## v2.6 修订后授权清单（替代 §8；其余条款同 v1）

| # | 授权 | 内容（相对 v1 的变化加粗） |
|---|---|---|
| A | 启动 7 容器（同 v1 顺序），结束仅 stop 本次启动者；**阶段 0 若 state.json 存在非终态历史任务 → 中止并报告，不隔离运行、不清队列** | 不动其他容器/镜像/卷/网络 |
| B | 同 v1（admin↔Manager DM + 四 Worker 房间；范围含交接/返工 revision/blocked 升级/收尾；只读导出） | 不新建房间、不发他处 |
| C | deepseek-chat；**软阈值 80%（240k token/48 请求/48min）触发"停止全部 5 个调用者"**；四上限（300k/60/60min/¥10）取最先达到；**阶段 0 优先配置网关配额为真硬控（若支持）；用量按全调用者合计、在途预记** | 不换模型/密钥 |
| D | 同 v1：仅测试仓新分支 `mergepilot-demo/rework-refund-20260915` + 1 个 OPEN PR；**分支边界=任务指令+token scope（请在 GitHub 核对 token 仅限该仓）**；不改验收测试 | 不 merge、不写 main |
| E | 同 v1（结束 stop 本次启动容器、快照对照；卷/历史/分支保留）；E2 默认**关** | — |

**回复格式**："A–E 批准（E2 开/关）+ token scope 已核对/按指令约束"。自然案例首次即通过则如实保留；Scenario B 默认不启用。

---

# 修订 v3（2026-09-15 第七轮：仅预算口径 / GitHub 权限 / 阶段 0 顺序。只读核查，未启动、未调用、未发消息、未写仓、未 push）

## v3.1 预算：先撤销、后两案

**撤销宣称**：v2 的"80% 停全部调用者"只构成**软预算控制**；不再宣称 300k token / 60 请求 / 60 分钟 / ¥10 "不会超限"；20% 余量不视为充分。

只读核查到的配置能力（来源：镜像内常量/脚本 `docker create+cp` 提取 + 源码 `223ddc2`，均未运行）：

| 能力 | 事实 |
|---|---|
| 每请求输出上限 | **Manager**：deepseek-chat `maxTokens=128000`（manager/configs/known-models.json，启动脚本写入 openclaw.json）。**Worker（copaw/agentscope oai schema）**：默认 `None`（无上限）；worker 的 openclaw.json 由 MinIO 同步，其值**待验证**（阶段 0 读取）。DeepSeek API 侧 deepseek-chat 单请求输出另有服务端上限（约 8K token），最终以网关/供应商返回为准 |
| 并发与自动重试 | 每 worker 并发 ≤10（`COPAW_LLM_MAX_CONCURRENT` 默认）、QPM 600（滑动窗）、重试 ≤3（退避 1–10s，仅进程存活期间）；manager 经 openclaw 网关 provider 走同一 Higress 路由。**最坏并发 5 调用者 × ≤10** |
| 统一计量 | **单一收口存在**：全部调用者经 Higress AI 路由（ctrl:8080；控制台 API `/v1/consumers`、`/v1/ai/routes`，管理员凭据即栈内 `AGENTTEAMS_ADMIN_USER/PASSWORD`）。用量统计能力（ai-statistics/控制台）**待验证**（阶段 0） |
| 网关/供应商额度拦截 | Higress 支持路由级限流与（视版本）token 配额插件；本嵌入版**是否可用待验证**（阶段 0 经控制台 API 查询/配置）。供应商侧（DeepSeek 账户）额度不在本栈控制内 |
| 在途请求与停止后计费 | `docker stop` 杀进程即停重试链；**在途请求会完成并计费**（流式受 `AGENTTEAMS_AI_STREAM_IDLE_TIMEOUT` 约束）；Higress 自身对上游 5xx 的重试默认行为未确认 |

**方案 A（可执行限制——限制位置明确，待验证项如实列出）**：阶段 0 在 Higress 控制台为 deepseek AI 路由配置：①请求速率限制（如 10 请求/分钟，覆盖全部调用者）；②若本版支持 token 配额插件 → 设 300k token 总额。限制位置=网关路由插件（单一收口，含 manager+4 worker）。待验证：控制台写权限、插件可用性、配额语义（命中后 429 → 进程内重试 3 次后放弃，计入请求预算）。**A 成立时上限才是硬的**；届时再按实测语义重算余量（不预设 20%）。
**方案 B（软预算——如实声明无法保证）**：无网关配额时，上限仅靠"≤5 分钟采样 + 80% 阈值停全部 5 个调用者"执行。**无法保证不超限，也无法保证最坏费用**：两次采样间理论突发上界受进程内 QPM 600×5 影响（现实中任务串行、并发 ≈1–2，但该界不作承诺）。参考量级：协议按预期执行时 ≤300k token ≈ ¥1–2；失控模式的费用上界**无法计算，明确写"无法保证"**。选 B 即表示接受该风险。

## v3.2 GitHub 权限：三层区分（实测 + 待确认清单）

| 层 | 实测事实 | 待你在 GitHub 后台确认（无法只读验证 token） |
|---|---|---|
| ① token 可访问仓库 | token 在 ctrl 容器 env（`AGENTTEAMS_GITHUB_TOKEN`），**值未读取**；平台层（AgentTeams 源码）无任何仓库/分支 allowlist | 该 token 是 classic PAT（scope=repo？可及多少仓库）还是 fine-grained（限定 `fastapi-boilerplate-demo`？） |
| ② 能否写 main / 合并 | **实测：`main` 无分支保护、无 rulesets**——仓库层无任何阻止；是否发生完全取决于 ① + ③ | 无需确认（已测）；如需仓库层硬边界见下"单独申请" |
| ③ 任务指令限制 | kickoff 指令限定唯一分支/操作；**这是提示词约束，不是权限控制**（v3 起明确不作强制依据） | — |

**最小权限方案（推荐）**：新建 fine-grained PAT 仅授予 `nghqqa/fastapi-boilerplate-demo`：Contents read/write、Pull requests read/write、**无 Administration、无 merge 权限需求**；替换 ctrl 的 `AGENTTEAMS_GITHUB_TOKEN`。**涉及 token 更换 = 权限变更，单独申请授权后执行**。仓库层加固（可选、单独授权）：给 `main` 加 ruleset（block force push + require PR）。
**不改 token 的替代**：现 token + 指令约束 + 事后提交审计（按 ② 实测，main 无保护——**如实声明该边界是指令性的**）。
**本地备选（零外部写）**：不建分支/PR——kickoff 要求各角色以**补丁文件（unified diff）**交付，Verifier 在操作员本地 checkout（`run-elem-20260915-01` 专属目录）上跑同一验收测试；GitHub 完全只读。真实模型输出、独立测试、Manager 状态链全部保留；仅失去"PR 页面"呈现。

## v3.3 阶段 0（修订：分层观察式；应对 reconcile 自动拉起）

**新事实（源码确证）**：`WorkerSpec.DesiredState()` 默认 **Running** → 若 worker/manager 已注册为 CR，**仅启动 `p14h2-wd-ctrl` 就可能被 reconcile 自动拉起全部团队容器**——"基础服务与调用者完全分开启动"不可行。安全边界：闲置 agent 不发起模型调用；任务恢复仅由事件唤醒（源码 + PHASE13.1 实证），我们在 kickoff 前不发任何任务消息。ctrl 内 supervisord 进程清单（minio/tuwunel/higress×5/element-web/mc-mirror/**agentteams-controller**）已提取留档，其中**无 LLM Agent 进程**。

阶段 0 顺序（每步有中止条件；不清队列、不改历史状态）：
1. **仅启动 `p14h2-wd-ctrl`** → 记录 reconcile 自动拉起了哪些容器（观察 2 分钟，docker ps + 日志）。
2. **只读检查（不启动其余容器；若已被 reconcile 拉起则直接检查）**：① Manager state.json（MinIO `mc cat`）：非终态历史任务或出现无法判读的排队/恢复字段 → **中止报告**；② worker openclaw.json 的 maxTokens（补 v3.1 待验证项）；③ Higress 控制台：读 `/v1/ai/routes`+consumers → 判定方案 A 可行性并（若可行且已授权）配置限流/配额；④ manager/worker 日志 grep 模型调用痕迹（kickoff 前出现任何调用 → **中止报告**）。
3. **启动本次调用者**：`agentteams-manager`（若未自动起）→ 观察 60s 无自发任务/模型活动 → 4 worker → element-web。
4. 获授权后 `agt llm-preflight`（计请求 #1）→ kickoff。

## v3.4 范围（不变，重申）

本轮 = AgentTeams 原生真实协作验证，**Manager 为状态权威**；不合并宣称 MergePilot Controller/PG 已接通真实 Worker；E2 关、Scenario B 不启用。自然案例首次通过如实记录；**只有实际观察到 测试 FAIL → Manager 状态 revision → 重新派发，才标记"真实返工验证成功"**；保存全部尝试，不为制造返工修改验收测试。

## v3.5 一次确认的授权文本（待批）

> **授权（AgentTeams 原生真实协作验证，run-elem-20260915-01）**
> ① 启动：`docker start p14h2-wd-ctrl`，允许 reconcile 自动拉起团队容器；随后按需 `docker start agentteams-manager / 4×worker / p14h2-wd-element-web`；结束仅 stop 本次启动或被自动拉起的容器。
> ② Matrix：admin↔Manager DM + 四个 Worker 房间（交接/返工 revision/blocked 升级/收尾），只读导出消息；不新建房间。
> ③ 模型：deepseek-chat；预算采 **方案 A**（阶段 0 配置 Higress 限流 10 req/min + token 配额 300k，若插件可用）/ **方案 B**（软预算：采样 ≤5min、80% 阈值停全部 5 调用者；已知无法保证上限，接受超限风险）；llm-preflight 计请求 #1；墙钟 60 分钟。
> ④ GitHub：**甲**=fine-grained PAT（仅测试仓、Contents+PR 读写）替换现有 token（单独授权）；或 **乙**=现 token + 指令约束 + 事后审计（已知 main 无保护）；或 **丙**=本地零写方案（补丁文件交付）。分支 `mergepilot-demo/rework-refund-20260915` + 1 个 OPEN PR（甲/乙）；不 merge、不写 main、不改冻结验收测试。
> ⑤ 中止条件（任一即停并报告，不清队列）：state.json 非终态历史任务/未知排队字段；kickoff 前观察到任何模型调用；方案 A 不可行且方案 B 未获接受；软阈值触发。
> ⑥ 清理：结束后 stop 本次涉及容器（对照启动前快照）；卷/消息历史/分支（如建）保留；证据入 `evidence/FINALS-ELEMENT-REWORK-20260915/`。E2 关闭。
