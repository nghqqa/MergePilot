# 决赛优化 · 事实核验表（2026-09-14）

> 口径：所有条目来自 `origin/main@db5944a` 与本轮分支 `feat/finals-organizer-response` 的实际代码/证据；行号以 main 为准。
> 四类状态：**已实现**（代码存在）· **有真实运行证据**（历史或本轮真实执行）· **合成/模拟**（夹具、回放、overlay）· **本轮新增**（本轮实现并实测）· **未执行**（需要另一环境或授权）。
> 代码支持 ≠ 已运行；历史证据不能证明新案例。

## 一、架构与运行时

| 事实 | 状态 | 依据 |
|---|---|---|
| 执行层 = AgentTeams（HiClaw）v1.2.3，CoPaw worker 容器（Manager + Reviewer/Fixer/Verifier） | 已实现 · 有真实运行证据（PR#1/2/3，2026-08-29/30） | `evidence/PHASE14-*`（SHA256 锁定）、`runtime-images-20260912`；本机仍有 11 个已退出的 p14h2 容器（未启动、未触碰） |
| 调度层 = Workflow Controller（PG 权威状态机 + outbox） | 已实现 | `tools/workflow-controller/controller.py`（3213 行） |
| 权限层 = Policy Gateway + GitHub MCP 隔离（PAT 不进 Worker） | 已实现 | `tools/policy-gateway/`、`tools/agentteams/gh_fix_branch.py` |
| 审计层 = PostgreSQL 29 表 / 13 迁移应用 / 13 角色 | 已实现 · 本轮真实 apply | 本轮离线 db-init 000→004 全量 apply：**29 表**；加 m9 后 **33 表** |
| 演示主面 = `demo-platform/`（零依赖 Node + React） | 已实现 · CI selftest | `.github/workflows/selftest.yml`；54 → **61** 项 |
| `tools/demo_console/`（ISOLATED_LIVE，showcase A/B/C） | 已实现（M7/M8 期，非决赛主面） | 无代码交叉引用 |

## 二、重点核实项

| 核实项 | 事实 | 依据 |
|---|---|---|
| **返工次数** | `MAX_VERIFY_ATTEMPTS=3`：verify FAIL 第 1、2 次退回 Fixer（fix attempt 2、3），第 3 次 → `HOLD/verify_max_hold`；即**最多 2 次返工**。BLOCKED 与 FAIL 同样消耗次数 | `controller.py:948, 1466-1483`；本轮机制验证场景 B/C 实测 |
| **角色拓扑** | 旧路径（`M4F_ONLY_MODE=0`）：review→fix→verify，NEXT_AGENT review→fixer、fix→verifier；只有 verify 完成校验 sender=verifier，review/fix 完成只校验 sender∈{reviewer,fixer}；Candidate/M5 严格路径校验 stage↔sender，但 **FAIL/BLOCKED 直接 HOLD，无返工循环** | `controller.py:1356-1358, 1405, 365-367, 902-933` |
| **版本绑定** | `revision_bindings` 一 run 一 revision、UPDATE/DELETE 触发器拒绝；`approvals.expected_head_sha` 锁 PR 头（TOCTOU）；`l2_create_ticket` 以 `run_pr_bindings` 为锚；`uq_active_ticket_per_binding_action` 同绑定同动作只允许一张活动票 | `m4f1_state.sql:65-76, 429-430`；`m3b_policy.sql:76`；`m3b_b4.sql:62-168`；本轮负向测试实测 |
| **审批终态** | PR#3 拒绝后再批准 → 409 `REJECTED_CASE_TERMINAL`（selftest 断言）；本轮 PR#4 版本闸门 `REJECTED_TERMINAL` 同语义 | `demo-platform/backend/lib/api.mjs`（approval 路由）；selftest |
| **运行时写入范围** | demo-platform 唯一磁盘写入 = `backend/rag-data/tool-spans.jsonl` 追加；审批 POST 只写内存 overlay（`runtime_write=false`）；证据目录只读；本轮所有 PG 写入只发生在本任务专用容器 `mp-finals-dbverify-pg` | `rag.mjs writeToolSpan`；`api.mjs demoOverlay`；容器标签 `mp.task=finals-opt-20260914` |
| **PR#4（改前）** | 三候选 a/b/c 每次 GET 重算：新 branch id + 12 条 tool-span 追加；人工门为静态字符串；无版本绑定、无修订回路 | 改前 `api.mjs GET /api/pr4`、`polardb_adapter.mjs` |
| **PolarDB / Branch** | 8 项接入门槛中 3 项硬编码 `ok:false`，LIVE 不可达；Branch = SIMULATED；PolarDB = NOT CONNECTED | `polardb_adapter.mjs evaluateLiveGates()` |
| **RAG（改前）** | 8 篇合成文档 / 9 chunk；CJK 单字 + 256 维哈希 + 无 IDF；tool-spans 604 行，其中 rag.retrieve **127 行仅 12 个不同 query_hash**；日志不存原文 | `rag.mjs`、`rag-data/tool-spans.jsonl` |
| **AgentLoop Trace** | 权威 Trace `fbf4a3ce…`（17.6s，LLM 24 / Tool 8 / 51,890 token）为历史真实云端记录（n=1，非实时） | `evidence/PHASE14-WINDOWS-AGENTLOOP-OTEL-*`、答辩稿 |
| **模型密钥** | benchmark adapters 从 `OPENAI_API_KEY/DEEPSEEK_API_KEY/MP_LLM_KEY_FILE` 读取；属付费调用，本轮未执行 | `benchmark/adapters/mergepilot.py:31-56` |

## 三、三方向改前差距 → 本轮状态

| 方向 | 改前差距 | 本轮状态 | 等级 |
|---|---|---|---|
| 一 · 多 Agent | 演示时间线全线性，无"验证失败→退回重修"案例；无可靠性对照矩阵 | 真实控制器 + 真实 PG + 真实测试驱动的返工闭环（A/B/C/D 四场景 23 项检查）+ `/rework` 演示页；可靠性对照 5 样本确定性层 | **机制验证**（Agent 输出受控；Element 交接未执行）；模型轴 NOT_EXECUTED |
| 二 · 数据库 | PR#4 静态重算；无版本绑定/修订回路/失效机制/方案包 | m9 子表 + `db_release_gate` + 真实 SQL 纵向闭环（FAIL 23502 → 补取信息 → PASS 11/11 → 绑定审批 → STALE）+ 迁移方案包 + PR#4 版本绑定人工门 | **真实 SQL 验证（ISOLATED_POSTGRES）**；非 Agentic Database 分支 |
| 三 · 可观测 | 只有观测→评估，无数据集/调优/回测后半环 | RAG 闭环：观测核验 → 18 族 54 查询按族切分 → 基线 → 网格调优 → held-out 单次回测 → 晋级 v2 + API/README 暴露版本 | **真实离线实验**，SYNTHETIC 语料，小样本 |

## 四、本轮真实执行的数字（以本轮输出为准）

- 临时 PG：`pgvector/pgvector:pg16`（image `7f58c9936b2e…`）、PostgreSQL 16.14；离线 db-init 000→004 apply OK → 29 表；m9 两次 apply 幂等 → 33 表；全新库 000→005 含 m9 → 33 表 / 6 新函数。
- DB 闭环：17 步 / 15 负向全部符合预期 / 闸门时间线 7 步；rev1 `SQLSTATE 23502`（代码测试 7/7 PASS）；rev2 11/11；补取信息：覆盖 120 / 不可回填 17 / 重复 2（12.4% ≤ 20% → 修订候选）；两次运行 `outcome_signature` 一致。
- 返工闭环：34 个事件注入真实 `process_event`；23/23 检查；两次运行签名一致 `bff63ce5…`。
- RAG：调优集 30 条 / held-out 24 条；基线 held-out hit@1 75.0% · hit@3 100% · MRR 0.8611；选中 `bigram+idf+hash4096`；回测 hit@1 91.7% · MRR 0.9583；改善 5 / 退化 1（F17-q1）/ 不变 18；泄漏检查 max Jaccard 0.258、0 对超阈；`report.json` 两次运行逐字节一致。
- 可靠性：5/5 决策正确、5/5 风险级别正确、误报 0、漏报 0、人工介入率 80%、保护检查全过；模型轴 NOT_EXECUTED。
- 测试：demo-platform selftest **61/61**；`tests/dbverify` 17、`tests/agentteams` 5、`tests/reliability` 8、`tests/m5_0/test_m5_0b_delivery_digest.py` 71 —— 全部通过（PG 门控用例在临时容器上真实执行）；文档命令 `pytest tests/demo_console tests/isolated_live tests/verification tests/release_delivery` 收集 **1490**（基线与分支相同）；全仓收集 3993。全量回归的通过/失败/跳过数字见终报（与 origin/main 基线在同一机器逐用例对照）。
- 前端：`/pr4`、`/rework` 在 390 与 1440 宽度 `scrollWidth == clientWidth`；既有 `/rag` 页在 390 宽度存在横向溢出（742 > 380，主干既有，未改）。
