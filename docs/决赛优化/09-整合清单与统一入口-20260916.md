# 09 · 整合清单与统一入口(2026-09-16)

> 本轮目标:以 p14-demo 的已验证设计/真实案例/P14 镜像为基线,选择性整合 finals-opt 新工程,完成真实 AgentTeams 案例、Element 可视交接、观测链路与演示同步。
> 结论:**已完成真实 PR #2 案例(VERIFIED PASS)**,统一演示入口已接入本次真实运行证据。本文是整合清单与启动说明的权威版本。

## 1. 两边差异与最终统一架构

| 维度 | p14-demo(P14 基线) | finals-opt(决赛优化) | 统一后 |
|---|---|---|---|
| 定位 | 复赛交付包:演示平台+109 个冻结证据目录+录屏 | 决赛优化工程:Git 工作树,真实 AgentTeams 实验、m9/PG/RAG 闭环、demo v2 | p14-demo=交付与证据档案入口;finals-opt=唯一 Git 工程源(演示与整合在此进行) |
| 编排 | CoPaw Leader(p14h2-copaw 网络副本) | 早期计划用平台 Manager 作派发者 | **统一为 CoPaw Leader 编排**(P14 已验证);平台 Manager 停用(heartbeat 失控风险) |
| 运行栈 | p14h2-wd-ctrl + p14h2-copaw-net(历史,保留) | elemiso 隔离栈(新卷/新网,无历史队列) | **elemiso 隔离栈**为本次运行载体,镜像与 P14 完全一致(223ddc2 系) |
| PR #2 证据 | PHASE14-WINDOWS-*(历史真实,SHA 锁定) | — | 历史回放保留 + **本次真实运行 FINALS-ELEM-PR2-LIVE-20260916(新)**,两条链并列、等级分列 |
| PR #3 证据 | PHASE14-WINDOWS-COPAW-HIGH-RISK-REJECT-*(历史,29 文件锁定) | — | 复用历史(本轮未重跑),演示中以 REJECTED 终态短案例呈现 |
| 支付幂等 | — | FINALS-REWORK-LOOP(CONTROL_PLANE_MECHANISM) | 保持控制面机制证据定位,不写成真实 PR 运行 |
| 数据库 | — | FINALS-DB-MIGRATION-LOOP(本地 PostgreSQL) | 附录/详情定位,不接 PolarDB(本轮禁令) |
| RAG | — | FINALS-RAG-LOOP(离线评估回测) | 附录/详情定位 |
| Demo | demo-platform(复赛 v1)+决赛路演 zip | demo-platform v2(real-PR-led 适配器+honesty contract) | **finals-opt 的 demo-platform 为统一演示入口**(含 v1 历史回放+新真实运行) |

## 2. 整合清单(来源 → 功能 → 依赖 → 目标 → 验证)

| # | 来源 | 功能 | 依赖 | 目标路径 | 验证方式 |
|---|---|---|---|---|---|
| 1 | 本轮运行 | **真实 PR #2 案例**(审查→人工门→修复→验证) | P14 镜像×3、elemiso 栈、DeepSeek key | `evidence/FINALS-ELEM-PR2-LIVE-20260916/`(SHA256SUMS 锁定) | SHA256SUMS 重算;demo 案例 hash_verified=true;GitHub API 运行前后核验 |
| 2 | P14 镜像 | 运行时(agentteams-embedded/manager/copaw-worker) | Docker Desktop | elemiso 隔离栈(新名/新网/新卷) | image ID: ae47995d209f / cafca0c1dc16 / cdc8f8a4ab8d 与 P14 证据一致 |
| 3 | P14 播种法 | CoPaw 项目存储播种(projectflow 官方路径+plan.md 解析器原生格式) | copaw 223ddc2-build1 内 task.py/projectflow | Leader 工作区 shared/projects/elemiso-pr2-gate | ready_nodes 读回返回 [pr2-review-1];委派/验收全链走通 |
| 4 | P14 消息协议 | kickoff/委派提及协议(正文首词 MXID + m.mentions) | copaw matrix 桥 | send_kickoff.py / send_admin.py(D:/mp-finals-tmp/elemiso-task-pr2/) | 修正版 kickoff 被 Leader 消费;委派事件与任务 meta event_id 一致 |
| 5 | finals-opt demo v2 | 统一演示入口(honesty contract+证据等级词表) | evidence-adapter | `demo-platform/backend/lib/demo_cases.mjs`(新增 caseD/caseE+新等级) | `/api/demo/cases` 返回 4 案例;前端动态渲染无需重建 |
| 6 | P14 历史证据 | 历史回放包(replayMaterials/fixAudit/fixVerify/finalLock/rejectDemo) | PHASE14 目录(已同步至 finals-opt evidence) | finals-opt evidence/ | 启动时完整性校验 160/160 files match |
| 7 | finals-opt 工程 | m9/PG/RAG/可靠性闭环 | 各 FINALS-* 目录 | 保持原位(附录定位) | 各目录自带 SHA256SUMS 与 summary |

**未整合/不整合项及原因**:PolarDB Agentic Database(本轮明令禁止接入);旧 p14h2 栈启动(避免 CR reconcile 恢复无关团队);平台 Manager 参与业务编排(与 CoPaw Leader 争用,且 heartbeat 失控);自动 merge/push(全程禁止)。

## 3. 统一启动说明

### 3.1 统一演示入口(离线,零外部依赖)
```bash
cd D:/goai/mp-worktrees/finals-opt-20260914/demo-platform
node backend/server.mjs
# → http://127.0.0.1:4173
# 默认案例 = 本次真实运行(fastapi-pr2-live-20260916);案例选择含:
#   fastapi-pr2-cwe22(历史回放) · fastapi-pr3-reject(人工拒绝·REJECTED 终态)
#   rework-payments(控制面机制) · db-migration-orders(附录)
```

### 3.2 Element 可视交接(房间历史保留,服务在即)
```bash
# Element:      http://127.0.0.1:18088  (homeserver http://127.0.0.1:18167, 用户 elemiso-admin,密码在栈内 env,由使用者自行输入)
# Higress 控制台: http://127.0.0.1:18001
# Controller API: http://127.0.0.1:18090
```
房间:团队房 `!RErK7WVs9iUeaszwho:elemiso-matrix:6167`(派发/风险/补丁/验证全可见);Leader DM `!VocOLrDhUMBcBjzIuY:elemiso-matrix:6167`(kickoff/人工门/最终报告)。

### 3.3 复现真实案例(端到端)
```bash
# ① 恢复计费调用者(CR state=Stopped → 改回 Running 由控制器重建容器)
docker exec elemiso-ctrl sh -c 'TOKEN=$(cat $AGENTTEAMS_AUTH_TOKEN_FILE); for w in leader reviewer fixer verifier; do curl -s -X PUT -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d "{\"state\":\"Running\"}" "http://localhost:8090/api/v1/workers/$w"; echo; done'
# ② 确认 4 容器 Running,网关路由 default-ai-route 的 allowedConsumers 含全部 worker
# ③ 预检(1 次调用):agt llm-preflight --api-key "$(cat key路径)" --base-url https://api.deepseek.com
# ④ 发 kickoff(脚本见 D:/mp-finals-tmp/elemiso-task-pr2/send_kickoff.py;正文首词 @leader + m.mentions)
# ⑤ 高危结论后人工门:向用户展示 findings,批准后发 send_gate_decision.py(写入 human-gate-approval.md)
# ⑥ 收尾:CR state=Stopped 停全部计费调用者(容器移除,房间/卷/CR 保留)
```
预算护栏(本轮口径):deepseek-chat;合计 ≤300 万–3000 万 token(见 AUDIT §1 口径说明)、≤6000 请求、≤¥10、≤120 分钟;软阈值 80% 停全部调用者;唯一计量权威=网关 ai_log(含 Manager+Leader+Workers 全部调用者)。

## 4. 已知限制(如实)

1. **copaw matrix 桥的提及派发在 consumer 空闲清理后不稳定**(上游行为):两次经带 @mention 消息/容器重启恢复;正式执行均由角色 agent 自主完成,操作员消息仅唤醒派发。
2. **团队房间上下文回放可唤醒非目标 worker**(本轮 Fixer 自启事故):需要操作员主动停工+审计留痕;根治需上游把"回放上下文"与"派发"分离。
3. **工具护栏审批超时按容器钟计算**:时钟跳变会把等待判为超时拒绝;本轮已记录并绕行(无 rm 命令)。
4. **本轮无真实返工样本**:PR #2 首次即 PASS(如实记录);返工机制由控制面(FINALS-REWORK-LOOP)与 PR #3 拒绝分支共同覆盖,两者均已分列标注。
5. 费用/token 预算为运行控制口径,非账单硬封顶;网关 proxyNextUpstream 的内部重试不可逐条观测。
