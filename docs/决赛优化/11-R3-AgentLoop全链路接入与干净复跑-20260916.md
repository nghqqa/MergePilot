# 11 · R3：AgentLoop 全链路接入 + 干净复跑（PR #2 R3-TRACED / PR #3 R2-TRACED）— 2026-09-16 执行记录

> **2026-09-17 追记（RAG-TRACED 轮 + 演示平台替换，见文末"十二"）**：应用户指令，RAG MCP 已挂进 worker 并
> 全链重跑两个案例（`FINALS-ELEM-PR2-RAG-TRACED/`、`FINALS-ELEM-PR3-RAG-TRACED/`），演示平台已替换为
> 这两个新案例（pr1 退役）。本文其余部分保留为 R3-TRACED（无 RAG 轮）的历史记录。

> 授权依据：任务书"MergePilot 决赛镜像重构 + AgentLoop 全链路接入 + PR #2/#3 干净复跑"（用户 2026-09-16 批准，
> AgentSpace "mergepilot" 华东1）；key 由用户现场提供。执行窗口 13:30Z–16:21Z，全程守候。
> 结论：**全部完成**——镜像 `copaw-worker:223ddc2-agentloop`（v2.2）构建成功；4 worker 一次创建零重启；
> PR #2 全链闭环（20m29s，门→Leader 新委派→Fixer→Verifier VERIFIED）；PR #3 拒绝分支（2m35s，零派发→blocked）；
> **701 span 全部直连 SLS 导出成功（0 失败）**；证据包 ×2 已入仓；全部计费调用者已停止（CR Stopped）。

## 一、交付物

| # | 交付 | 状态 |
|---|---|---|
| 1 | 镜像 `agentteams/copaw-worker:223ddc2-agentloop`（image `8a6b8995ccc2`，v2.2 延迟 patching） | ✅ 构建文件入 `release/agentloop-copaw-image/` |
| 2 | PR #2 R3-TRACED 全链闭环（全部 span 覆盖） | ✅ `evidence/FINALS-ELEM-PR2-R3-TRACED/`（62 文件 + SHA256SUMS） |
| 3 | PR #3 R2-TRACED 拒绝分支（零派发三重核验） | ✅ `evidence/FINALS-ELEM-PR3-R2-TRACED/`（41 文件 + SHA256SUMS） |
| 4 | AgentLoop 控制台截图 | ✅ 已入包：`FINALS-ELEM-PR3-R2-TRACED/agentloop-console-screenshot-pr3-window.png`（6 条 Trace 与本地事件链逐秒互证，详见该包 README 第四节；key 经操作员决定**暂不轮换**，后续复用） |
| 5 | 停止全部计费调用者 | ✅ 4 CR Stopped @16:21:02Z；停机后网关零新调用 |
| 6 | 代码/文档入 Git（不 push） | ✅ 本次提交 |

## 二、埋点方案（v2.2 延迟 patching，方案 A 强化版）

两代教训与稠点表详见 `release/agentloop-copaw-image/README.md`。要点：
`.pth` 导入零副作用（仅 stdlib + watcher 线程）→ watcher 轮询 `/etc/agentloop-otel.json`（镜像默认
`enabled:false` 无密钥）→ 运行时经 stdin 改写开关（~2s 生效，零重启）→ 90s 宽限后仅 patch 已加载模块
（绝不主动 import）→ 稠点全部为运行时按名查找（Toolkit.call_tool_function / MatrixChannel._was_mentioned+send
（custom-channel 顶层模块名）/ _send_matrix_room_message / _notify_task_assignment / RetryChatModel.__call__ /
OpenAIChatModel.__call__ / CoPawAgent.reply）→ span 覆盖 tool.\*、matrix receive/send、agent turn、
genai llm（流式收口 + usage tokens）、委派 eventId。导出带状态审计行（OTEL_EXPORT SUCCESS n=）。

## 三、运行事实（证据锚点）

- **PR #2**（`1dedf5e1…`，branch demo/high-risk-human-gate）：kickoff `$wqTKbyLT…` 15:35:31Z → 委派 review
  `$n_sZQTvvi…`（5s）→ Reviewer 15:48:00Z FINDING_CONFIRMED/HIGH/CWE-22/HVR:YES → 门批准（操作员现场）
  15:53:52Z → **Leader 新 fix 委派 `$tVLWTi47TF…`（10s，R2 验收口径）** → Fixer +13/−7（sha256
  `674356fc…16081` 与 R1/R2 一致）→ Verifier VERIFIED（5 逃逸向量含前导 `/` 全 400；断言反转如实记录）→
  终报 15:56:00Z completed。墙钟 20m29s。span 快照 524、导出 0 失败。
- **PR #3**（`ad267a6e…`，branch demo/high-risk-human-reject）：kickoff `$ks17JyF9pF…` 16:06:40Z → Reviewer
  **51 秒**确认 HIGH/CWE-78 未认证 RCE（`;` `|` 换行 反引号 `$(...)` root 级执行）→ 门拒绝（操作员现场）
  16:09:07Z → Leader 8 秒落实绑定效应并终报 **PROJECT_BLOCKED_HUMAN_REJECTED**；PR3 窗口 @fixer/@verifier
  mention = **0**（房间切片 + plan `[-]/[!]` + 任务目录缺失三重核验）。墙钟 2m35s。
- 用量：PR2 90 调用 / 6.22M 输入（98% 缓存）/ 30.6K 输出 ≈¥1.5–3.3；PR3 32 调用 / 2.39M（99%）/ 9.6K
  ≈¥0.6–1.3。PR2 上界略超 ≤¥2（与 R2 同性质），PR3 预算内；权威以 DeepSeek 控制台为准。

## 四、事故如实登记（摘要，全文见 PR2 包 AUDIT.md）

I1 v2.1 Toolkit 包装器形状错误 → 第一次 kickoff（15:11:34Z）失败（≈2 次 LLM 调用），根因修复 + 真实
Toolkit 路径测试后二次 kickoff；I2 正式 worker 两次重建（v2.1→v2.2），最终栈自 15:27:41Z 起一次创建零重启，
全部正式证据在其上产生；I3 Windows 保留端口段致金丝雀一次卡 Created（删除重建即好）；I4 `delete worker` 会
新建房间 → 正式栈全程用 `agt update`；I6 Reviewer 跨轮记忆（MinIO 持久家目录）如实披露；I8 controller
`Stopped` 语义移除 worker 容器/auth 卷（CR 与 ctrl 数据卷保留，证据停机前采集）。

## 五、待办（用户侧）

1. ~~AgentLoop 控制台截图~~ ✅ 已完成并入包（2026-09-17：6 条 Trace 逐秒互证，见 PR3 包 README 第四节）。
2. license key：操作员决定**暂不轮换**（后续运行复用；key 仅存工作树外 `D:/mp-finals-tmp/agentloop.key` 与栈内开关文件，未入 Git/证据包）。
3. 如需 push（分支 `feat/finals-organizer-response`），另行授权。


## 十二、追记（2026-09-17）：RAG MCP 接入 + 全链重跑 + 演示平台替换

> 授权：用户 2026-09-17 指令"pr2 和 pr3 都接入 RAG 全链重跑，替换演示平台两个 traced 案例；
> 预算不设限；人工门默认按先例执行（PR #2=批准、PR #3=拒绝），中间不参与"。执行窗口 16:30Z–17:30Z。

### A. RAG 接入（P14 资产复用 + v2.2 延迟纪律）

- 镜像 `copaw-worker:223ddc2-agentloop-rag`（image `443e73375932`）：在 -agentloop 之上叠加
  `rag-mcp-server.mjs`（P14 原样复用，MCP stdio，citation-only）+ `zz_agentloop_rag.py`（延迟 hook：
  .pth 零副作用 → watcher 轮询 `/etc/agentloop-rag.json` → 90s 宽限 → patch `CoPawAgent.register_mcp_clients`）
  + 开关文件（默认 disabled）。
- 调试如实记录（隔离容器内定位，均未影响正式栈）：① StdIOStatefulClient 必须显式 `await connect()`
  （toolkit.register_mcp_client 校验连接态）；② `cwd: ""` 致 node spawn FileNotFoundError（去除该参数）；
  ③ 修复后以端到端 MCP 握手（connect→list_tools→rag_retrieve）+ canary 验证才进入正式运行。
- RAG 服务：宿主 `:4184`（`rag-live-server.mjs`，零依赖；契约与平台 `/api/rag/search` 一致）。
  **知识型语料（8 文档/12 chunk）**：仅组织安全规范/CWE 定义/修复模式，**零案例结论**——保护 Reviewer 独立性
  （注意 :4174 平台 forwarder 的语料含案例摘要，存在答案泄露风险，故本轮刻意分离）。
  服务端审计 `rag-tool-spans.jsonl` 只存 query_hash/source_refs。

### B. 两轮运行（真实执行，门按授权自动投递）

| | PR #2 RAG-TRACED | PR #3 RAG-TRACED |
|---|---|---|
| run_id / project | run-elem-pr2rag-20260917-01 / elemiso-pr2rag-gate | run-elem-pr3rag-20260917-01 / elemiso-pr3rag-reject |
| 墙钟 | **4 分 15 秒**（17:16:30→17:20:45Z） | **1 分 19 秒**（17:25:07→17:26:26Z） |
| 结果 | review(FINDING_CONFIRMED/HIGH/CWE-22) → 门批准 → **9 秒后新 fix 委派** → VERIFIED → completed | review 38 秒（HIGH/CWE-78 + RAG 引用）→ 门拒绝 → **9 秒后** PROJECT_BLOCKED_HUMAN_REJECTED，零派发三重核验 |
| RAG 实际调用 | reviewer/fixer/verifier 各 1 次（先复现后检索，明示"references only"） | reviewer 2 次（cwe-78-command-injection + command-execution 规范） |
| 补丁 | sha256 `674356fc…16081` **第 4 次独立一致**；本轮机理由：RAG 组织规范 file-path-containment.md 即该实现模式 | 无修复（拒绝分支） |
| 追踪 | 647 span 全部直连导出（rag_retrieve span ×8、0 失败）；探针 4×HTTP 200 | 同栈同镜像；审计窗口切片 0 失败 |
| 用量 | 80 调用 / 6.65M 输入（94% 缓存）/ 24.6K 输出 | 31 调用 / 2.87M（99%）/ 9.2K |

- 停机：17:28:12Z 全部 CR Stopped；停机后网关零新增调用。
- 证据包：`FINALS-ELEM-PR2-RAG-TRACED/`（62 文件）、`FINALS-ELEM-PR3-RAG-TRACED/`（53 文件），SHA256SUMS 锁定，
  精确密钥扫描 0 命中（157 文件）。

### C. 演示平台替换（两案例阵容）

- `evidence-adapter/evidence.mjs`：注册 `finalsPr2RagTraced` / `finalsPr3RagTraced`（两包均 SHA256SUMS 锁定，
  平台启动时实时重算校验）。
- `evidence-adapter/replay-provider.mjs`：整体重写 buildPR2/buildPR3 为 RAG 运行锚点（16/9 个时间线事件全部
  带真实 event_id/时间戳/source_ref；启动完整性门校验全部引用文件存在）；**pr1 退役**（案例数 3→2）；
  `replayAudit` 组件表更新为当前栈真实状态（AgentLoop/OTel=VERIFIED 全程直连、RAG MCP=VERIFIED、
  PolarDB=NOT_IMPLEMENTED 如实保留）；新增探针对比解析器（直接解析 SHA256SUMS 锁定的 verification.md：
  修复前 3 逃逸向量 200 泄露 → 修复后 400、合法 200、缺失 404）。
- `shared/data-contract.json` 枚举收缩为两案例；`backend/test/selftest.mjs` 全面更新（两案例断言、
  RAG 事件存在性断言、探针 400 断言、门纪律断言）——**selftest 57/57 全绿**。
- 前端（src 编辑 + vite 重建）：pr1 全部引用清零，案例标签更新为"RAG+追踪版"，证据级别 REAL_EXECUTED。
- 后端重启后 API 实测：/api/cases=2 案例；pr2 probe 200→400、image=agentloop-rag；pr1=404；pr3
  fix-1=rejected/verify-1=locked 带原因；新 bundle（index-CMg_PVtL.js）服务中。

### D. 环境事件（如实）

- 重启平台服务时误用 `taskkill /IM node.exe` 波及宿主全部 node 进程（平台 backend/forwarder 一并被杀）；
  已按原启动方式恢复（4173/4174 验证 200）。处置改进：后续按 PID 精确终止。
- 期间一次自测请求误入平台 forwarder 审计文件，已精确移除该行（619→618 行）。
