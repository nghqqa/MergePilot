# Element 真实交接（返工验证）· 授权清单与只读准备（2026-09-15）

> 状态：**ELEMENT_NOT_EXECUTED / BLOCKED（等待授权）**。本文只做两件事：列出一次真实 AgentTeams/Element 返工验证需要的**具体授权项**，以及**已经完成的只读准备**。未获授权前不启动、不停止、不修改任何团队容器/镜像/卷/网络，不调用付费模型，不写测试仓。
> 只读预检：`python tools/agentteams/element_preflight.py`（只 inspect，不读取任何环境变量的值）。

## 一、现有环境盘点（只读，2026-09-15）

| 组件 | 容器 | 镜像 | 当前状态 | 端口（127.0.0.1） |
|---|---|---|---|---|
| AgentTeams 嵌入控制器（Matrix homeserver + MinIO + API；挂 docker.sock 拉起 worker） | `p14h2-wd-ctrl` | `agentteams/agentteams-embedded:223ddc2` | Exited | 6167 / 8090 / 9000 |
| Leader / Manager（持 LLM provider、模型、AI 网关配置） | `agentteams-manager` | `agentteams/manager:223ddc2` | Exited | — |
| CoPaw Reviewer / Fixer / Verifier / manager worker | `agentteams-worker-p14h2-copaw-worker-{reviewer,fixer,verifier,manager}` | `agentteams/copaw-worker:223ddc2-build1` | Exited | — |
| openclaw 对照 worker ×4（p14h2 A/B 实验，本次不需要） | `agentteams-worker-p14h2-wd-worker-*` | `agentteams/worker-agent:223ddc2` | Exited | — |
| Element Web（人类可见的交接界面） | `p14h2-wd-element-web` | `vectorim/element-web:latest` | Exited | 18088 |
| 网络 | `p14h2-wd-net`、`p14h2-copaw-net` | — | 存在 | — |

预检结果：所需 7 个容器均存在（Exited）；两个网络存在；4 个端口空闲；配置变量**名**齐全（`AGENTTEAMS_MATRIX_URL/DOMAIN`、`AGENTTEAMS_DEFAULT_MODEL`、`AGENTTEAMS_LLM_PROVIDER`、`AGENTTEAMS_WORKER_ROOM_ID`、`AGENTTEAMS_GITHUB_TOKEN` 等；**值未读取**）；返工案例文件齐全。

**结构性事实**：p14h2 栈里**没有** MergePilot 的 Workflow Controller / Policy Gateway / 审计 PG 容器。因此：
- **第一层（原生）**：只启 p14h2 栈 → 返工由 AgentTeams leader（projectflow/taskflow）驱动，证据 = Element 消息 + MinIO 任务结果 + GitHub 分支提交；**不是**由 MergePilot PG 控制器退回。
- **第二层（PG 权威）**：另起 MergePilot 隔离栈（docker compose：controller / gateway / audit-pg…）并让控制器以 admin 账号加入同一房间消费 `TASK_COMPLETED` —— 需要额外授权（A5）与 Matrix 凭据配置。
- 真实 LLM 下**返工次数不可控**：Fixer 可能一次修对（0 次返工）。届时如实记录，不为"制造返工"篡改输入；机制层证据仍以 `FINALS-REWORK-LOOP`（CONTROL_PLANE_MECHANISM）为准。

## 二、需要的具体授权（逐项，互不包含）

| # | 授权项 | 具体内容 | 影响范围 / 风险 |
|---|---|---|---|
| A1 | **团队与容器** | `docker start`：`p14h2-wd-ctrl` → `agentteams-manager` → 4 个 CoPaw worker → `p14h2-wd-element-web`（按依赖顺序）。**不启动** openclaw 对照 worker。运行结束后只 `docker stop` 本次启动的容器，不删除、不修改任何容器/镜像/卷/网络 | ctrl 挂载 docker.sock，可拉起新 worker 容器；MinIO/Matrix 数据卷 `p14h2-wd-data` 会新增数据 |
| A2 | **Matrix 房间** | 使用 worker 配置中的 `AGENTTEAMS_WORKER_ROOM_ID`（值由操作员在 Element 中确认后告知；本文不读取）。操作员账号：`AGENTTEAMS_ADMIN_USER`（值不读取）。是否允许新建专用房间：**否**（沿用既有房间，避免改团队配置） | 房间历史会新增本次消息；Element 截图由操作员手工截取 |
| A3 | **模型与费用** | provider/model 取 `AGENTTEAMS_LLM_PROVIDER` / `AGENTTEAMS_DEFAULT_MODEL`（历史为 deepseek-chat 经 AI 网关）。**建议费用上限**：总 token ≤ 300k（历史单次全流程 51,890 token；按最多 3 次 fix + 3 次 verify 估算），墙钟 ≤ 60 min，超限由操作员在 AI 网关/控制台停止；请确认是否有网关侧硬预算 | 付费调用；不可撤销 |
| A4 | **测试仓与写入范围** | 仓库 `nghqqa/fastapi-boilerplate-demo`（历史 PR #1–#3）。写入范围：**新建 1 个分支** `mergepilot/rework-payments-<日期>`（放入 `tools/agentteams/rework_case/base/*`）+ **新建 1 个 PR**（保持 OPEN）；Fixer 只能向该分支提交；**禁止** merge、禁止写 main、禁止改验收测试文件。使用的凭据：ctrl 的 `AGENTTEAMS_GITHUB_TOKEN`（值不读取；请确认其权限范围仅限该仓） | 外部可见的 GitHub 写入 |
| A5 | （可选）**PG 权威层** | 启动 MergePilot 隔离栈（`docker compose` 7 服务）并配置控制器加入同一房间（需要 admin Matrix token 写入控制器 env）。若不授权，本次证据停留在第一层 | 新增 7 个容器 + 卷；需要一次性配置 |

## 三、已完成的只读准备（无需授权）

- `tools/agentteams/element_preflight.py`：只读预检（容器状态 / 网络 / 端口 / 变量名 / 案例文件），输出 `authorization_required` 清单。
- `tools/agentteams/rework_case/element_messages.md`：kickoff 与三角色回帖模板，每条消息携带 run_id、PR、head_sha、测试报告与证据路径；退回语义与证据采集清单。
- 案例文件（`rework_case/base|attempt1|attempt2`、`reviewer_findings.json`）与验收测试就位；Verifier 的裁决只能来自 `python -m unittest discover -p 'test_*.py'` 的真实结果。
- 证据目录约定：`evidence/FINALS-ELEMENT-REWORK-<日期>/`（Matrix 事件导出、MinIO 结果、GitHub 提交链、成本计量、操作员截图），SHA256SUMS 锁定；等级标签 **REAL_EXECUTED（AgentTeams 原生）** 或 **REAL_EXECUTED + CONTROL_PLANE（第二层）**，与机制验证证据分列。

## 四、运行步骤（授权后，供操作员执行；本文不执行）

1. `docker start` 按 A1 顺序；等待 ctrl 健康（6167/8090/9000 可达）；打开 Element `127.0.0.1:18088` 登录操作员账号，确认房间。
2. 按 A4 创建分支与 PR（不 merge）。
3. 在房间发送 kickoff（模板 §0），记录 run_id 与 PR 号、head_sha。
4. 观察三角色回帖；每次 Fixer 提交后记录新 head_sha；Verifier 必须给出 `VERDICT=` 行与测试报告路径。
5. 达到 PASS 或 HOLD（上限）后停止；导出 Matrix 事件、MinIO 结果、GitHub 提交链、成本；操作员截图。
6. `docker stop` 本次启动的容器（不删除）；生成 SHA256SUMS；写入响应文档，等级按实际情况标注。
