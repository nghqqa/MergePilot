# 05 · D:\goai 目录只读盘点与用途索引（2026-09-15）

> 性质：**只读盘点**。本轮未移动、未删除、未重命名任何路径；所有“建议”均为未执行项，需另行确认。
> 采集方式：`ls / du / stat / git worktree list / git status --porcelain`（Git Bash），密钥目录只记录文件名与大小，不读取内容。
> 总量：约 44 GB，顶层 22 个目录 + 13 个文件。上一版盘点见根目录 `workspace-inventory-2026-08-13.md`。

## 1. 代码仓库与工作树（同一远端 `nghqqa/MergePilot`）

| 路径 | 分支 @ HEAD | 状态 | 用途 / 说明 |
|---|---|---|---|
| `mergepilot-os/` (7.4G) | `feat/agentloop-otel-genai-poc` @ 12deea5 | **脏 25 项**（7 修改 + 18 未跟踪：`.env.mergepilot`、evidence/agentloop-poc/PHASE10–13、tools/cli/*、tests/cli/） | 主仓库主工作区。本地 `main` 停在 a5a33a0（08-27），落后 `origin/main`（db5944a，PR #229）。脏改动为历史遗留，本轮未触碰 |
| `mp-worktrees/finals-opt-20260914/` | `feat/finals-organizer-response` @ 11b73f8 | 领先 origin/main 10 提交；脏 4 项（本轮新增文档 03/04、element_preflight.py、element_messages.md，待提交） | **本轮决赛优化工作树**（从 origin/main 创建）。未 push |
| `mp-worktrees/delivery/` | `feat/offline-image-delivery-v2` @ 53a505d | 干净；领先 origin/main 1 | 离线镜像交付 v2（09-13） |
| `mergepilot-readme-image-fix/` (27M) | `fix/readme-architecture-typography` @ ad1aea4 | 干净 | README 架构图排版修复工作树 |
| `mp-benchmark-preview4/` (19M) | `benchmark/preview4-formal-20260826` @ 6d4029f | 脏 6 项 | 复赛 preview.4 正式基准工作树 |
| `preview-accept/` (43M) | detached @ d77a12e | 脏 1 | 复赛验收副本（remote 指向本地 `mergepilot-os`） |
| `preview3-accept/` (852M) | `final-smoke` @ 379744d | 脏 1 | 复赛 preview3 冒烟副本（remote 指向本地 `mergepilot-os`） |

本地分支（近期）：`release/v0.2.1-bundle`、`docs/runtime-adoption-guide`、`fix/python-verify-fresh-clone`、`docs/runtime-migration-story`、`docs/runtime-env-scope`、`fix/start-demo-bat-encoding`（均 09-11/12）。

## 2. 决赛材料与演示（`p14-demo/`，8.8G）

| 子路径 | 大小 | 用途 |
|---|---|---|
| `evidence/` | 8.4G（110 目录 + 196 文件） | PHASE14–18 证据目录（每个 ~100K–10M）；**大头是 3 个镜像 tar**：`img-manager.tar` 3.0G、`img-worker-agent.tar` 3.0G、`img-embedded.tar` 2.3G |
| `submission/` | 87M | 全部提交包：`GOAI-MergePilot-OFFICIAL-FINAL-20260830(.zip)`、`MergePilot-路演全套-20260831(.zip)`、`MergePilot-答辩移交包-v2-20260901`、`MergePilot-决赛路演材料-20260913`（zip 75M 在上级目录）、**`MergePilot-决赛路演材料-20260914-增补`（本轮维护）**、路演 A/B 两版 PPT 与对比说明、答辩稿 |
| `submission/MergePilot-决赛路演材料-20260914-增补/07-PPT/` | — | 本轮生成的瑞士风网页 PPT **初稿**（8 页 + preview/ + assets/）。用户已决定后续换其他模型重制，此处仅作参考，不再投入 |
| `demo-platform/` | 52M | 决赛演示平台（零依赖 Node 后端 + React 控制台），工作树内有同源副本 |
| `FastAPI-boilerplate/` | 220M | 测试目标仓库本地副本（`nghqqa/fastapi-boilerplate-demo`） |
| `recording/` | 8.9M | 演示录制素材 |
| `p14-base/`, `agentloop-preflight/`, `project-analysis/` | <5M | 基线快照、AgentLoop 预检、项目分析笔记 |
| 根文件 | — | `README.md`、`create-finals-ppt.mjs`（旧 pptx 生成脚本）、`removed-containers-backup-20260912.json`（09-12 容器清理备份）、`分支清单备份-20260911.txt`、`err.txt` |

## 3. 运行时镜像与大归档

| 路径 | 大小 | 用途 | 备注 |
|---|---|---|---|
| `wsl-archives/MergePilot-Test-20260828-233137.tar` (+.sha256) | 19G | WSL 发行版整机归档（08-28，WSL 退役审计时导出） | 单一大文件；是否保留需用户决定 |
| `temp/runtime-images/` | 7.0G | `agentteams-manager-223ddc2.tar`（3.2G，另有 part-00/01 分卷 3.2G）、`copaw-worker-223ddc2-build2.tar`（0.99G）、SHA256SUMS、release-notes | 分卷与整包重复约 3.2G |
| `p14-demo/evidence/img-*.tar` | 8.4G | 见上表 | 与 `temp/runtime-images` 内容可能重叠（未比对哈希） |
| `mergepilot-demo-recording/` | 477M | 复赛演示视频制作全套（帧、字幕、TTS 脚本、成片 mp4） | |
| `archive/` | 34M | 08-13 清理时归档：evidence-backups / historical-materials / logs-and-outputs / repositories / submission-builds / virtualenvs | |

## 4. 小型工作目录与配置

| 路径 | 用途 |
|---|---|
| `MergePilot-复赛首次提交-20260826/`（+ .zip 15M + .sha256） | 复赛首次提交包原件 |
| `m9-demo/MergePilot-Demo/`, `p12/agentteams-v123/` | M9 演示、AgentTeams v1.2.3 安装/试验目录 |
| `mp-gh4-direction-apply/`, `mp-gh4-tx/` | GH4 方向重连试验（4 个 rewire-real-*）与事务日志 |
| `temp/` 其余 | `mp-wt-*`（6 个空壳工作目录 8K）、`ppt-review(-v2)`、`official-selftest2`、`roadshow-verify`、`element-config.json`、`mf.json`、`repo-paths.txt`、`demo-server.log` |
| `.impeccable/`, `.mergepilot/`, `.zcode/` | 工具缓存 / e2e 配置 / 计划文件 |
| 根文件 | `HANDOFF-MergePilot-20260913.md`（交接）、`agentloop_relay_log.jsonl`、`build_appeal_doc.ps1`、`build_log_evidence_image.ps1`、4 份 `workspace-cleanup-*-2026-08-13.md` + `workspace-inventory-2026-08-13.md`、`e2.txt`、`o2.txt`(0B) |

## 5. 敏感目录（仅登记路径，未读取）

| 路径 | 内容类型 | 要求 |
|---|---|---|
| `mp-gh4-secrets/` | GitHub App 私钥 `app-private-key.pem`、installation-id、webhook-secret、Matrix token/users、mcp-bridge PAT 等 16 个文件 | **禁止进入任何提交包 / 仓库 / 截图**；本轮材料包 SHA256SUMS 不含此路径 |
| `secrets/deepseek_api_key.txt` | 模型 API key | 同上 |
| `mergepilot-os/.env.mergepilot`（未跟踪） | 运行时环境变量 | 已被 .gitignore 规则覆盖前提下保持未跟踪；提交前用 `git status` 复核 |

## 6. 本轮（决赛优化）新增/使用的临时资源

| 资源 | 位置 | 处置 |
|---|---|---|
| 临时 PostgreSQL 容器 `mp-finals-dbverify-pg`（端口 55432，label `mp.task=finals-opt-20260915`） | Docker | 收尾时 `docker rm -f` + 删除匿名卷 |
| 密码文件 `pg.pw` | `D:\mp-finals-tmp\` | 收尾时删除 |
| 运行日志、补丁脚本、截图（`ppt-shots/`、`chrome-prof*`） | `D:\mp-finals-tmp\`（位于 D 盘根，不在 goai 内） | 保留可复查；无敏感内容（chrome-prof* 为空白 headless 配置） |
| conda 环境 `goai`（Python 3.10.21，mcp 1.28.1） | conda envs | **按用户要求保留** |
| 静态服务 `python -m http.server 8791` | 进程 | 收尾时停止 |

## 7. 观察与建议（全部未执行，仅供后续决策）

1. **重复的大体积镜像 tar**：`temp/runtime-images` 内整包与分卷并存（≈3.2G 重复）；`p14-demo/evidence/img-*.tar` 与之可能同源。建议先 `sha256sum` 比对再决定去重，任何删除需单独授权。
2. **19G WSL 归档**是单个最大文件；若 WSL 路线已退役且 `.sha256` 已校验，可考虑迁移到外部存储（未执行）。
3. `mergepilot-os` 主工作区 25 项脏改动含 PHASE10–13 未跟踪证据目录与新 CLI 工具，属历史工作现场；建议由原作者决定提交或归档，本轮不动。
4. 本地 `main` 落后 `origin/main`（a5a33a0 → db5944a）；如需在主工作区继续开发，先 `git fetch && git merge --ff-only origin/main`（未执行）。
5. `temp/mp-wt-*` 六个 8K 空壳目录与 `o2.txt`(0B) 可能是残留物；不影响任何流程，是否清理待确认。
6. 两处密钥目录建议后续统一到操作系统凭据管理或加密卷；至少确认 `mp-gh4-secrets/` 不在任何同步/备份脚本的范围内。
