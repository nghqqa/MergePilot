# 决赛优化 · 评委意见响应（2026-09-14）

> 格式：**评委意见 → 实现 → 证据 → 边界 → 未完成项**。每条实现都标注证据等级；缺少真实 Agent 或 Agentic Database 验证的地方不宣称已完整满足。
> 证据等级定义：
> - **真实 SQL 验证（ISOLATED_POSTGRES）**：迁移与断言在独立 PostgreSQL 克隆库上真实执行；不是 Agentic Database 分支。
> - **机制验证**：真实控制器代码 + 真实 PostgreSQL + 真实测试执行；Agent 的语义输出（findings / 补丁）由 harness 提供，无 LLM，无 Matrix/Element。
> - **真实离线实验**：确定性算法在合成语料上真实运行，结果可逐字节复现。
> - **历史真实运行证据**：PHASE14（2026-08-29/30）AgentTeams/CoPaw 真实运行的回放，与本轮新案例无关。
> - **NOT_EXECUTED**：需要另一环境或授权，本轮未做，不据此宣称。

---

## 方向一 · 多 Agent 协同

| 评委意见 | 实现 | 证据 | 边界 | 未完成项 |
|---|---|---|---|---|
| 选 1～2 个有明确预期与验收条件的案例；保留一次修复未通过验证、被退回的过程 | 案例"支付幂等 bug"：Reviewer 依据 F1（`payments.py:29`，L1）→ Fixer 尝试 1（按 payment_id 去重，看似合理）→ Verifier 跑验收测试 **FAIL**（`test_second_payment_request_for_same_order_is_idempotent`）→ 控制器建 `fix#2` + outbox「回退修复」→ 尝试 2（按 order_id 幂等）→ **PASS 5/5** → task PASS | `evidence/FINALS-REWORK-LOOP-20260914/report.json`（stage_runs：review#1 / fix#1 / verify#1 FAIL / fix#2 / verify#2 PASS；两份 unified diff；测试日志；task_runs 快照 `verify_attempt=1`、`last_error=verify FAIL; retry fix attempt=2 (1/3)`）；演示页 `/rework` | **机制验证**：状态转换由真实 `controller.py::process_event` 计算、VERDICT 由真实测试结果决定；Reviewer/Fixer 输出为受控输入 | 真实 LLM 驱动的 Reviewer/Fixer 输出；CoPaw 容器内运行 |
| 由真实验证结果驱动状态转换，不用预置时间线 | 同上；harness 中没有任何预置 VERDICT——每次 verify 前真实执行 `unittest`，结果写进 Matrix 事件正文 | 同上；`tools/agentteams/rework_loop_harness.py::run_acceptance_tests` | 同上 | — |
| 保存风险依据、两次补丁、代码 SHA、测试命令与结果、返工原因、任务 ID、消息关联、最终处置 | 全部保存：findings JSON、`patches/*.diff`、每次尝试的 git 树 SHA（`git mktree`）、测试命令与输出、outbox 正文、run_id ↔ stage_events(event_id/sender/type/stage/status) | 证据目录 + SHA256SUMS；`/rework` 页"事件链"表 | 树 SHA 是内容身份，不是远端提交 | — |
| 验证重试上限、超限 HOLD、上下文不足、冲突时人工升级 | 场景 B：3 次 FAIL → `HOLD/verify_max_hold`，不派发第 4 次；场景 C：缺验收测试 → `VERDICT=BLOCKED` → 退回 → 上限 HOLD（转人工）；场景 D：非 verifier 的 verify、重复 event_id、缺 VERDICT 快照、非 admin 提交 → 状态机不动 | 同上 report `scenarios.B/C/D`，23/23 检查 | 观察：被忽略事件在 `stage_events` 标 PROCESSED 而非 ERROR（审计可读性可改进，未改控制器） | Candidate/M5 严格路径无返工循环（`_m5_advance_verify` 直接 HOLD）——已如实记录，未改 |
| 在 Element 展示同一任务的真实交接 | 未做 | — | 需要启动现有 AgentTeams 栈（本机 11 个退出容器）——属"现有 AgentTeams 环境操作"，按授权边界未启动 | **阻塞项**：Element 真实交接展示；不伪造截图 |
| 新案例不得冒用 PR#4 / PHASE14 身份 | 新案例 run_id `run-rework-*`，evidence 目录 `FINALS-REWORK-LOOP-*`，与 PHASE14 目录、PR#4 完全分离；`/rework` 页顶栏明示等级 | 目录命名 + 页面等级标签 | — | — |
| 小规模可靠性对照（长/跨文件、小上下文、诱导输入、不同模型） | `benchmark/reliability`：5 固定样本 + 固定评价；确定性层（diff_parse / risk_classify / sast_scan）真实执行：5/5 决策正确、0 误报 0 漏报、人工介入 80%、保护检查全过（截断视图不得 PASS、注入文本零影响、破坏性迁移必须人工） | `evidence/FINALS-RELIABILITY-20260914/report.md` | 模型轴 **NOT_EXECUTED**（付费调用需授权；报告含精确复现命令） | 不同模型能力对照、模型层 FP/FN/成本 |
| 后台展示模型/Skill 版本、检索与上下文链接、测试依据、Agent 轨迹、审批与最终状态 | PR#4 页"运行元数据/服务状态"：平台版本、Node、运行时长、RAG 策略版本（v1→v2）、m9 schema + 闸门函数、PG 版本与试验实例、控制器模式与 MAX_VERIFY_ATTEMPTS、Agent 运行时、边界、三证据目录 SHA256 校验；`/api/health.finals`、`/api/finals/evidence` | 页面 + API + selftest | — | per-task 实时 Trace 仍 NOT_AVAILABLE（历史口径不变） |
| 演示一次版本回退或失败重试；部署配置与服务状态 | 失败重试：场景 B 的 attempt 序列、DB 闭环的 attempt 1/2/3；版本回退：追加提交后旧批准 STALE + 方案包 `04-rollback.sql`；服务状态：`/api/health`、`/api/ops`、运行元数据面板 | 同上 | — | 真实生产回退未执行（不应在演示环境执行） |
| PPT 聚焦审修质量与可靠性 | 见《02-PPT压缩指引》 | — | — | PPT 文件本身未重制（指引 + 讲稿顺序已给） |

## 方向二 · 数据库迁移安全

| 评委意见 | 实现 | 证据 | 边界 | 未完成项 |
|---|---|---|---|---|
| 迁移验证纳入 PR 验收；展示"代码测试通过但迁移因历史数据失败"，修订后重新验证 | `tools/dbverify/run_migration_loop.py`：基线（10,000 订单、137 NULL、2 重复支付）→ rev1 代码测试 **PASS 7/7** → 迁移 **FAIL `SQLSTATE 23502`** → **补取信息**（真实 SQL 查 legacy 覆盖 120 / 不可回填 17 → 规则 ≤20% → 修订候选）→ rev2（同候选 revision 2，parent=rev1）代码测试 PASS → 迁移 **PASS 11/11 断言**（含旧 worker 兼容插入探针、重复支付被数据库拒绝） | `evidence/FINALS-DB-MIGRATION-LOOP-20260914/report.json`、`run.log`、`case/`；PR#4 页"候选 A · 真实 SQL 验证链" | **真实 SQL 验证（ISOLATED_POSTGRES）**：`CREATE DATABASE … TEMPLATE dbv_baseline` 克隆的独立 PostgreSQL；**不是 PolarDB Agentic Database 分支**，PolarDB 保持 NOT CONNECTED | Agentic Database 真实分支验证（无授权环境） |
| 验证结果与人工批准绑定到准确的代码与数据版本；PR 更新后旧结论/审批失效；用事务和唯一约束处理重复回调、重试、并发审批；不能只在 UI 显示 STALE | `tools/audit-db/m9_migration_verification.sql`：在不可变 `revision_bindings` 与既有 `approvals` 上挂 4 张**不可变**子表（`data_baselines` / `migration_candidates`(head_sha 触发器强制等于 revision 头) / `migration_verifications`(UNIQUE candidate,baseline,attempt) / `approval_verification_bindings`(PK ticket)）；`l2_bind_verification` 事务内校验 5 项匹配；`db_release_gate(ticket, target_data_digest)` **每次调用重算 11 项**，结果不落库。实测：重复回调幂等、异摘要回调拒绝(23505)、并发绑定只有一个成功、既有 `uq_active_ticket_per_binding_action` 阻止第二张活动票、追加提交 → `STALE_SUPERSEDED_BY_NEW_REVISION`、目标数据摘要不符 → `TARGET_DATA_DIGEST_MISMATCH`、失效后迟到 PASS 回调不能复活 | 同上 report `negative_tests`（15/15）与 `gate_timeline`；`tests/dbverify`（17）；m9 幂等两次 apply、全新库 000→005 含 m9 → 33 表 | 扩展现有模型，未新建 run 状态机；`revision_bindings` 未改 | 网关执行路径（`l2_claim_ticket`）尚未强制调用 `db_release_gate`——本轮由运行器在执行前调用；控制器/网关接线待下一步 |
| 交付经过验证的迁移方案而非只交付补丁；试验分支合并 ≠ 生产发布 | 运行器在 rev2 验证通过后生成 `release/migration-plans/orders-schema-change/candidate-a.rev2/`：README（适用版本：树 SHA / 脚本摘要 / 基线摘要 / 验证记录 / PG 版本）、01-preflight、02-migrate、03-postcheck、04-rollback、manifest、SHA256SUMS；README 明写"试验库通过 ≠ 生产发布" | 目录 + PR#4 页 ⑦ | 方案包由真实验证结果生成，绑定摘要 | 生产 PolarDB 上的受控执行（不应在演示中做） |
| 缺少上下文 → 补取信息或升级人工 → 后续动作改变 | 运行器 `S5b_context_fetch`：明确"缺什么"（137 单归属）、"查到什么"（真实 SQL）、事先声明的规则、决定与下一步动作；PR#4 页 ③ 展示 | 同上 | 规则阈值 20% 为本案例声明值 | — |

## 方向三 · 可观测优化闭环

| 评委意见 | 实现 | 证据 | 边界 | 未完成项 |
|---|---|---|---|---|
| 观测→评估→数据集→优化→回测 的完整闭环真实 case | `demo-platform/backend/experiments/rag-loop/`：① 观测核验：604 行 tool-span，rag.retrieve 127 行仅 12 个不同 query_hash（span 行数≠样本）；② 数据集：18 语义族 54 条标注查询，**按族切分**调优 30 / held-out 24，泄漏检查 max Jaccard 0.258；③ 基线 v1：held-out hit@1 75.0% / MRR 0.861；④ 调优集网格 12 策略选 `bigram+idf+hash4096`；⑤ held-out **单次**回测：hit@1 91.7% / MRR 0.958，改善 5 / **退化 1**（F17-q1）/ 不变 18；⑥ 按事先声明的晋级规则晋级为生产默认 `v2`，API/README 暴露版本与证据 | `evidence/FINALS-RAG-LOOP-20260914/report.json`（两次运行逐字节一致）、`queries.v1.json`、`timings.json` | **真实离线实验**；语料 SYNTHETIC/REDACTED（8 篇 / 9 chunk）；小样本，结论不外推到企业语料 | 企业语料上的重复实验；per-task 云端 Trace 关联 |

## 横向：材料、合同与测试数字

| 项 | 实现 | 证据 |
|---|---|---|
| 数据合同 | `shared/data-contract.json` 无代码消费者（核实）；更新为 3 案例 / critical / CWE-78 + 决赛端点，并新增 selftest 与真实 `/api/cases` 对齐 | selftest 第 61 项 |
| 测试数字 | selftest 54 → **61**；文档命令收集 1490（基线与分支相同）；全仓收集 3993；主干既有失败（同机基线 52 个：docker/fake-docker 布线、README 断言 1461 过期等）已逐用例对照，本轮仅引入并修复 1 个（交付面登记） | 终报"实际执行的测试" |
| 交付面 | M5-0B 交付面登记本轮正式文件（95 → 130 文件），清单再生成，71/71 | `tests/m5_0` |
| 界面区分 | `/pr4`：真实 SQL 证据回放（ISOLATED_POSTGRES 标签）与 SIMULATED 三候选分区；人工门标 `REPLAY ACTION — NO RUNTIME WRITE`；`/rework`：等级 MECHANISM VERIFICATION，Element 交接 NOT_EXECUTED 标签 | 页面 |

## 未完成项总表（诚实清单）

1. **Element 上的真实交接展示**（需启动现有 AgentTeams 栈 + 授权）— 未执行。
2. **真实 LLM 驱动的 Reviewer/Fixer/Verifier 跑新案例**（付费调用）— 未执行。
3. **Agentic Database（PolarDB）真实分支验证** — 无授权环境，未执行；以真实 SQL 验证 + 降级声明回应。
4. **模型能力对照轴**（不同模型 / 上下文预算下的 FP/FN/成本）— 运行器与样本已备，NOT_EXECUTED。
5. **网关执行路径强制调用 `db_release_gate`** — 函数与测试已备，控制器/网关接线待做。
6. **PPT 文件重制** — 只交付压缩指引与讲稿顺序。
7. **既有 `/rag` 页移动端横向溢出** — 主干既有，未改。

---

## 本轮二 · 收口与证据升级（2026-09-15）

### 新增：授权执行路径强制过闸（评委：验证回写与授权执行都必须检查当前版本）

| 项 | 实现 | 证据 | 等级 |
|---|---|---|---|
| 最终授权执行点 | Policy Gateway 的 `l2_claim_ticket`（APPROVED→EXECUTING 的 CAS）在 m9 §5.7 被替换为带 `p_target_data_digest` 的版本：票据若绑定了迁移验证，先算 `db_release_gate`，不 valid → `RAISE P0001 DB_RELEASE_GATE_REFUSED`，**不更新状态、不产生 execution_id**；未绑定票据行为逐字节不变 | `tools/audit-db/m9_migration_verification.sql` §5.7；离线 `001-init.sql` 同步；全新库引导 33 表、唯一 6 参签名 | LOCAL_REAL_SQL |
| 网关透传与映射 | `gateway.py`：`release_data_digest` 作为网关验证参数（排除在 args_hash 之外、不转发上游，格式错误 DENY `RELEASE_DIGEST_INVALID`）；claim 包装函数把 DB 侧拒绝分类为 `GATE_REFUSED` → DENY `DB_RELEASE_GATE_REFUSED`（区别于 CLAIM_MISMATCH / DB_ERROR） | `tools/policy-gateway/gateway.py`；`tests/dbverify/test_gateway_claim_wrapper.py`（在 conda `goai`、mcp 1.28.1 下**真实导入模块**执行；3.9 下如实跳过） | LOCAL_REAL_SQL（SQL）+ REAL_EXECUTED（网关代码单测）|
| 最小权限修正 | 真实 PG 抓到：`l2_*` 的 SECURITY DEFINER owner `mergepilot_l2_owner` 对新表无 SELECT、对闸门无 EXECUTE → 只授予这两项 | S12 首跑失败日志 → 修正后 23/23 | LOCAL_REAL_SQL |
| 负向测试（本轮新增 8 项） | 旧 head（STALE）claim 拒绝 · 目标数据摘要变化 claim 拒绝 · 未批准 claim 拒绝 · **过期票据** claim 拒绝（gate TICKET_EXPIRED）· **并发 claim 只有一个 EXECUTING** · 已 EXECUTING 再 claim 0 行（防重复执行）· 未绑定票据 CLAIMED（行为不变）· 网关包装函数映射 GATE_REFUSED；加上原有：重复回调幂等 / 冲突回调拒绝 / 迟到回调不复活 / 并发绑定 | `evidence/FINALS-DB-MIGRATION-LOOP-20260914`：20 步、**23/23 负向**、闸门时间线 8 步（含 TICKET_EXPIRED）、签名 `5647b166…` 两次一致 | LOCAL_REAL_SQL |
| 边界 | 这是**本地 PostgreSQL（ISOLATED_POSTGRES 克隆库）验证，不是 Agentic Database 验证**；claim 成功后本层无上游 GitHub 写入，票据以 `l2_fail_ticket("no upstream in this tier")` 如实关闭 | report `S12_claim_executed_then_closed` | LOCAL_REAL_SQL |

### 演示与文档

| 项 | 实现 | 等级 |
|---|---|---|
| `/rag` 390px 横向溢出 | 只修两类实际超宽元素（长文本 chip 换行、文档表内滚），作用域限定 RAG 页 ≤720px；数据与口径不动；`/rag`、`/pr4`、`/rework` 在 390/1440 无溢出 | REAL_EXECUTED（浏览器实测）|
| PPT | 压缩为三页主讲（角色职责与证据交接 / 失败返工与人工升级 / 数据库迁移安全与评估闭环）+ 附录；等级标签逐版块标注，禁止混写 | 文档（PPT 文件未重制）|

### 逐项状态标签（终态）

| 项 | 标签 |
|---|---|
| 三条安全路径 PR#1/2/3（历史 AgentTeams/CoPaw 运行回放） | REAL_EXECUTED（历史，SHA256 锁定）|
| AgentLoop 权威云端 Trace | REAL_EXECUTED（历史，n=1）|
| 数据库迁移纵向闭环（失败→补取→修订→PASS→绑定审批→失效→claim 过闸） | LOCAL_REAL_SQL |
| 迁移方案包 | LOCAL_REAL_SQL（由真实验证生成）|
| 多 Agent 返工闭环（A/B/C/D） | CONTROL_PLANE_MECHANISM |
| RAG 观测→评估→数据集→优化→回测 | REAL_EXECUTED（SYNTHETIC 语料）|
| 可靠性对照确定性层 | REAL_EXECUTED（SYNTHETIC 夹具）|
| 可靠性对照模型轴（真实 LLM，不同模型配置） | NOT_EXECUTED（付费调用未授权）|
| PR#4 三候选对照 | SYNTHETIC |
| PR#4 人工门 overlay | SYNTHETIC（REPLAY overlay，NO RUNTIME WRITE，回放真实 SQL 证据）|
| Element 上的同 PR 真实交接 | BLOCKED（需启动 AgentTeams 栈 + 授权；ELEMENT_NOT_EXECUTED）|
| PolarDB Agentic Database 分支验证 | NOT_EXECUTED（PolarDB NOT CONNECTED）|
| 生产 PolarDB 受控执行 | NOT_EXECUTED（不应在演示环境执行）|

## 本轮三 · 合并前审查与真实交接准备（2026-09-15）

### 合并前审查（三个问题的结论与落点）

| 审查问题 | 结论 | 实现 / 证据 | 等级 |
|---|---|---|---|
| 迁移票据是否**必须**绑定验证？ | 是，且现在是 fail-closed：一个 run 只要登记了迁移候选（`migration_candidates`），其票据未绑定验证就 claim → `DB_RELEASE_GATE_REFUSED: MIGRATION_VERIFICATION_REQUIRED`；普通 PR（无候选）行为逐字节不变 | m9 §5.7；负向 `claim_refused_when_migration_run_unbound`、`plain_ticket_claim_unchanged` | LOCAL_REAL_SQL |
| 目标数据摘要的**可信来源**与**缺省行为**？ | 缺省（不带摘要）对已绑定票据一律拒绝（`TARGET_DATA_DIGEST_REQUIRED`），不存在“默认放行”；摘要只能由执行方在目标库上用唯一规范算法现算（`tools/dbverify/data_digest.py`，与基线登记共用同一实现，`--expect` 不符 exit 3）；网关只校验 64-hex 格式、不转发上游、排除在 `args_hash` 之外 | 负向 `claim_refused_without_target_digest_for_bound_ticket`；迁移方案包 README 步骤 2 重写 | LOCAL_REAL_SQL |
| claim → 执行之间的**版本一致性**？ | 闸门在 claim 时以当前 head / 当前摘要重算；执行前再算一次（`recompute_before_migrate_matches_bound_digest`）；claim 后目标库被改动一行即被发现（`data_drift_after_claim_detected`）→ 停止，不进入迁移 | 27/27 负向、闸门时间线 8 步、签名 `0689f544…` 两次一致；网关在 conda `goai`（3.10.21 / mcp 1.28.1）下真实导入 `MODULE_IMPORT` | LOCAL_REAL_SQL |
| 遗留 3 个失败测试（compose digest 断言） | 根因是 f167762 之后的真实策略（pgvector 以可离线加载的 TAG 运行；注册表 digest 通过 `MERGEPILOT_DECLARED_PG_IMAGE` **声明**给 preflight 并被闸门校验；`image-set.json` 是唯一真值）没有被测试编码。修法是让 3 个测试与 compose 注释**编码该策略并交叉核对 image-set**，不是改断言让它变绿 | `tests/isolated_live/test_one_click_containerization.py`、`tests/release_delivery/test_image_delivery.py`、`docker-compose.yml` | REAL_EXECUTED（单测）|

### 测试隔离修复（同一解释器组合运行时暴露）

| 现象 | 根因 | 修复 | 结果 |
|---|---|---|---|
| dbverify 环路测试在 3.9 下断言 `MODULE_IMPORT` 失败 | `tests/isolated_live/test_m8a2a_pr_fixture.py` 在模块顶层向 `sys.modules` 注入裸 `mcp` 桩且不清理，使 `import mcp` 在 3.9 “成功” | 桩改为 `setUpModule` 安装 / `tearDownModule` 移除；`test_phase1d` 的加载器自带 `mcp.types` 替身，不再依赖泄漏 | 组合运行 dbverify 正确走 AST 回退并如实记录 |
| phase1d 5 个 healthcheck 测试连接被拒 | `run_migration_loop` 把 `tools/policy-gateway` 插入 `sys.path` 后不移除，后续 `import healthcheck` 解析到网关的探测脚本 | 加载器 `finally` 移除路径并恢复 `POLICY_FILE` | 5/5 通过 |
| `tests/gh_app/test_e2e_cli_wiring.py::TestStatusStopCleanupWiring` 3 个 rc=3 | CLI 前置检查 `DISTRO_NOT_REGISTERED`：本机 WSL 发行版 `MergePilot-Test` 已在 PHASE14 退役（`wsl -l -v` 仅 docker-desktop / Ubuntu-22.04）。分支对 CLI 的唯一改动是 m9 迁移文件名 5 行，与此无关 | **未修改**（宿主环境依赖、与本轮无关，如实记录） | 既有失败 3 个 |

组合运行数字：Python 3.9（dbverify + isolated_live + release_delivery + gh_app + agentteams + reliability + rag）**1815 通过 / 3 失败（上表既有）/ 15 跳过**；conda `goai` 3.10（dbverify + agentteams + isolated_live + release_delivery）**949 通过 / 0 失败 / 9 跳过**；demo-platform selftest **61/61**。

### 真实交接与资源确认（只做准备，未操作现有团队、未调用付费模型）

| 项 | 本轮产出 | 等级 |
|---|---|---|
| Element 真实返工交接 | `docs/决赛优化/03`：A1 容器与团队启动顺序 / A2 Matrix 房间 / A3 模型与费用上限 / A4 测试仓写入范围 / A5 可选 PG 权威层，逐项待授权；`tools/agentteams/element_preflight.py` 只读预检（容器/网络/端口/环境变量**名**）；`rework_case/element_messages.md` 消息模板（每条带 run_id / PR / head_sha / 测试报告 / 证据路径） | BLOCKED（授权清单已备）|
| PolarDB Agentic Database | `docs/决赛优化/04` 资源确认单 D1–D7 + 最小验证步骤；与本地 SQL 证据分列 | NOT_EXECUTED（NOT CONNECTED）|
| 工作区盘点 | `docs/决赛优化/05` 只读盘点与用途索引；未移动、未删除任何路径 | 文档 |
| PPT | 已用 guizang 瑞士风技能生成 8 页网页版**初稿**（校验器通过、逐页视觉检查通过、离线加固），位于材料增补包 `07-PPT/`；按项目方决定后续换其他模型重制，**本初稿不作为最终交付** | 初稿 |
