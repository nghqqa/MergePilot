__pycache__/
*.pyc
*.pyo
.env
*.env
.DS_Store
*.tmp
.tmp-*
.mergepilot/

# preview package build output (large OCI tar, rebuilt on demand)
dist/
# demo-platform 前端预构建产物例外：零依赖 clone-and-run 需要它随仓库分发
!demo-platform/frontend/dist/
release/preview/logs/
release/preview/manifests/

# frontend build dependencies (installed locally to rebuild demo-platform/frontend/dist; never committed)
demo-platform/frontend/node_modules/

# Local model/API credentials used only for authorized offline/isolated runs.
key.key
run-secrets/

# console frontend build deps (dist is committed, node_modules is not)
console/frontend/node_modules/
.impeccable/

# Mimosa 安全钩子本地状态
.mimosa/
