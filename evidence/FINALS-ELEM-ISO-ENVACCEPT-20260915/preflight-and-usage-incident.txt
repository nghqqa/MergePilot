# Preflight result + unplanned usage incident (the most important finding)

## Authorized preflight (exactly ONE invocation, 13:52:25Z)
- command: agt llm-preflight --provider openai-compat --base-url http://elemiso-controller:8080/v1
           --model deepseek-chat --api-key <manager consumer key via shell var, not echoed> --retries 2 --timeout 30
- result: HTTP 404 (Spring-format body, path /v1/chat/completions) = Higress console fallback;
  AI route NOT matched (single-label Host elemiso-controller dropped by envoy ingress host validation)
- upstream requests: 0. provider tokens billed by this invocation: 0. no retry (404 not transport error)

## Root cause + in-scope fix (no further preflight run)
- route domains [] (match-all) applied via console PUT at ~13:56:05Z (200)
- key-less probes (cannot reach upstream): Host=elemiso-controller / :8080 / aigw-local.agentteams.io -> all 401 (route matched, key-auth)

## UNPLANNED PROVIDER USAGE (autonomous stack behavior, NOT operator-initiated)
Timeline right after data-plane activation:
1) 13:56:20.376Z  src=172.19.0.2 (ctrl; Go-http-client)  route hit, upstream 117.163.60.130:443,
   bytes_received=95, response_flags=DC (downstream disconnect), ai_log={"chat_round":1} (no usage fields)
   -> controller provisioner onboarding/verification probe (provisioner.go:1542 "POST <AIGatewayURL>/v1/chat/completions
      with the manager's bearer"). 219 preceding 404 retries (13:37:01-13:56:15, ~5s interval, same source)
      were this mechanism failing against console-fallback 404.
   billing: stream aborted early; usage not recorded; assume <= negligible but UNVERIFIED
2) 13:56:25.885Z  src=172.19.0.3 (manager; OpenAI/JS 6.26.0)  HTTP 200, streaming
   model reported by provider: "deepseek-flash"; input=26021 (cached 0), output=196, total=26217
3) 13:56:28.090Z  src=172.19.0.3 (manager)  HTTP 200, streaming
   input=26381 (cached 26112), output=334, total=26715
   -> manager openclaw onboarding/welcome flow (welcomeSent). Manager then posted its welcome message
      to the Manager room (visible in matrix-rooms-export-sample.json).

Total planned-authorization usage (preflight): 0 upstream requests.
Total unplanned upstream usage: 3 requests (1 aborted probe + 2 manager welcome calls),
~52,932 billed-ish tokens recorded by gateway ai_log for #2/#3.
Cost estimate: well under CNY 0.2 at DeepSeek list prices; authoritative figure = user's DeepSeek console.
Model name "deepseek-flash" is what the gateway ai_log recorded as provider-reported model for a
deepseek-chat request; recorded as observed, not verified against DeepSeek docs.

## Authorization framing (honest)
User authorized: ONE llm-preflight (up to 3 HTTP retries). These 3 upstream requests were NOT that
preflight and were not separately authorized; they were produced autonomously by stack components
(controller onboarding retry + manager welcome) once the route became live. Reported as a deviation.
Mitigation for any future phase: welcome/onboarding calls are part of manager bootstrap; phase-2
budgeting must include them (and the route's proxyNextUpstream attempts=3 multiplier).
