# 凭据清理核验窗口（2026-09-15，用户单次授权；未调用模型、未发消息、未执行 preflight、未手动启动 Manager/Worker）

## 时间线（UTC）
- 14:45:35Z  docker start elemiso-ctrl（唯一启动的容器）
- +8s        Higress 控制台可达（:8001）；同一次 exec 内完成：
             1) 只读列出路由状态：1 条 default-ai-route，domains=[]，
                allowedConsumers=[manager, worker-reviewer, worker-fixer, worker-verifier]（无敏感字段）
             2) 只读列出 /data/configmaps/ 文件名（仅名称；未做删除前 sk- 扫描——按授权仅列路径）
             3) DELETE /v1/ai/routes/default-ai-route -> HTTP 204；GET /v1/ai/routes -> total=0
- 随后       provider 凭据清理：
             GET /v1/ai/providers/openai-compat -> 存在，token_count=1（值未读未输出，仅计数），
             openaiCustomUrl=https://api.deepseek.com
             DELETE /v1/ai/providers/openai-compat -> HTTP 204；GET -> HTTP 404（凭据对象已删除）
- 删除后     /data/configmaps/ 全目录 sk- 模式计数 = 0（仅计数扫描；删除前的磁盘存放形态未捕获，
             provider 对象存储于控制面内部存储的推断标注为"未证实"）
- 观察窗 75s  网关访问日志本窗口（start_time>=14:45:35Z）chat/completions 条目 = 0
             （连控制台兜底 404 都没有：WelcomeSent=true，探测循环不再运行，manager 空闲）
             → "无路由时上游请求为零" 成立（本次为直接零观测；历史 19 分钟 404 兜底零上游为旁证）
- 容器行为    elemiso-manager 由 reconciler 自动拉起（cr 内 WelcomeSent=true，未发生任何模型调用；
             事故文档 §四 已备案的必然行为，非手动启动）；3 个 worker 保持 Exited（CR phase=Sleeping）✓
- 14:48:24Z  docker stop elemiso-ctrl -> docker stop elemiso-manager
- 14:48:31Z  终态：5 声明容器全部 Exited(0)；全机 running=0；
             inventory 不变（containers=16 volumes=28 networks=6）；旧 11 容器全部仍 Exited

## 中止条件核对
- 清理路径不明确：未发生（路由/provider 均一次删除成功）
- 清单外资源：未出现（窗口内仅 ctrl+manager 运行，manager 为已备案 reconcile 行为）
- 未知任务：未出现（workers 保持 Sleeping，无新 CR）
- 任何上游调用：未发生（本窗口网关 0 条模型路径请求）

## 凭据状态结论
- 旧（已撤销）DeepSeek key 的最后栈内存留点 = 控制面 provider 对象 openai-compat（token_count=1）
  → 已于本窗口删除（DELETE 204 / GET 404）
- /data/configmaps/ 磁盘文件中 sk- 模式计数 0（删除后扫描）
- 工作树根 deepseek.key：未在本窗口触碰（由用户自行处置）
