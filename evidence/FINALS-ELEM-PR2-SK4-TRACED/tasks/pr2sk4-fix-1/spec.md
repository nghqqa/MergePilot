pr2sk4-fix-1 / run-elem-pr2sk4-20260918-01 - Fixer minimal fix, local patch only (attempt=1). Human security gate: APPROVED (2026-09-18T15:59:35Z).
1) taskflow(ack_task) taskId "pr2sk4-fix-1".
2) Follow your role contract at ~/task/ROLE-CONTRACT.md and the parameters in ~/task/PR-METADATA.md; clone in a NEW directory (never rm -rf) and checkout head SHA 1dedf5e1992c950557064d8f4fb9039d1523deb3 (verify with git rev-parse HEAD). Note: the repo conftest requires non-empty Google OAuth env values to import (provide dummy values via shell environment only; never modify repo files). Base the fix on the Reviewer finding summary posted in this room: path traversal CWE-22 in demo_download of backend/src/interfaces/api/v1/demo_high_risk.py (L28 DEMO_FILES_DIR base dir, L41 os.path.join(DEMO_FILES_DIR, name) with no normalization/containment, L42 FileResponse serves the resolved path; '../' sequences escape the base dir; no auth dependency on the route). Use /opt/venv/standard/bin/python (fastapi/httpx/pytest preinstalled); plain `python` lacks them.
3) Change ONLY the file(s) the Reviewer flagged: backend/src/interfaces/api/v1/demo_high_risk.py. Acceptance behavior: (a) names containing ../ or equivalent traversal sequences are rejected (HTTP 404 or 400) and no content outside the base directory is ever returned; (b) legitimate files inside the base directory still return 200 with content; (c) missing files return 404.
4) Self-check: /opt/venv/standard/bin/python -m py_compile passes. Generate patch: git diff --no-color -- backend/src/interfaces/api/v1/demo_high_risk.py > ~/shared/tasks/pr2sk4-fix-1/attempt-1.diff (mkdir -p first); compute sha256sum of the diff.
5) taskflow(submit_task) taskId "pr2sk4-fix-1" result status="SUCCESS", summary contains FIX_APPLIED + SELF_CHECK_PASSED, notes = [patch sha256, normalization method, boundary check method, error strategy].
6) Reply in THIS team room mentioning @leader: FULL diff in a triple-backtick diff block + patch sha256 + the three explanations; last line exactly: TASK_COMPLETED: run-elem-pr2sk4-20260918-01-fix attempt=1
Constraints: no test file changes (frozen); zero GitHub writes; submit when done without waiting for reminders.