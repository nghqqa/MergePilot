{
  "phase": "Phase 14.2H-WD-COPAW-HIGH-RISK-PR-DEMO",
  "recorded_at": "2026-08-29T14:44:00+08:00",
  "check_type": "high-risk-demo-flaw-design",
  "result": "PASS (the demo case was created safely and is real/reproducible)",
  "flaw": {
    "type": "Path Traversal / Arbitrary File Read (CWE-22 class)",
    "file": "backend/src/interfaces/api/v1/demo_high_risk.py",
    "endpoint": "GET /demo/download?name=<user-controlled>",
    "vulnerability": "os.path.join(DEMO_FILES_DIR, name) without normalization or containment check - '../' sequences escape the demo base directory",
    "severity": "HIGH (arbitrary file read within the process's readable scope)",
    "safety": "no real credentials; no persistence; no reverse shell; no destructive code; throwaway temp files only in the repro tests"
  },
  "repro_tests": [
    "backend/tests/unit/test_demo_high_risk_path_traversal.py::test_high_risk_path_traversal_reproduces (../ escape returns the outside file, 200 + content)",
    "backend/tests/unit/test_demo_high_risk_path_traversal.py::test_high_risk_path_traversal_depth (deeper nesting still escapes)"
  ],
  "fix_shape_for_later": "containment check (resolved path must be inside the base dir; reject otherwise) + the tests inverted to assert rejection"
}
