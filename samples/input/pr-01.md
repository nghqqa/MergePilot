# Fixture PR · 01 · 密钥泄漏 + SQL 注入(主演示)

## PR 元数据
- **PR 标题**:feat: add user lookup API
- **仓库**:`mergepilot/demo-service`(Mock)
- **分支**:`feature/user-api` → `main`
- **作者**:dev-alice
- **改动文件**:`user_service.py`(新增)

## 提交给 coordinator 的消息(把下面整段粘进 coordinator 私聊)

```
【PR 事件】repo=mergepilot/demo-service  PR #42  feature/user-api→main  by dev-alice
标题:feat: add user lookup API
改动:新增 user_service.py

请对这条 PR 执行完整闭环:派 reviewer 审查 → fixer 处理 → verifier 验证 → 给我 merge/hold/reject 报告。

def get_user(name):
    API_KEY = "sk-live-1234567890abcdef"
    import sqlite3
    conn = sqlite3.connect("db.sqlite")
    return conn.execute("SELECT * FROM users WHERE name='" + name + "'").fetchall()
```

## 期望结果(验证用,不粘贴)
- **F-001** security / critical / **L2**:硬编码生产密钥 `sk-live-*`(应脱敏、走环境变量、吊销旧密钥)
- **F-002** security / critical / **L2**:SQL 注入(字符串拼接 → 参数化查询)
- **F-003** quality / medium / **L0**:sqlite3 连接未关闭(→ with 上下文)
- 预期裁定:**hold**(F-001/F-002 需人审),fixer 对 L2 只出方案不执行;批准后才应用 + 最终复验通过
