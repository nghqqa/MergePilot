# case_retrieval 注入示例（占位符，**不含真实值**；真实值由部署者本地密钥管理注入）
# 目标面：reviewer（及 leader，如需）容器的环境。注入方式 = controller/Worker CR
# 或受控启动包装，属共享环境变更——未经授权不执行。

# case 库只读 DSN（占位：全部由部署者替换；不落仓库、不落日志）
MERGEPILOT_CR_PG_DSN=postgresql://cr_reader:SUBSTITUTE_AT_DEPLOY@elemiso-case-pg:5432/cases

# 方式 A（env 直填）：显式 repo 范围
# MERGEPILOT_CR_REPO_SCOPE=nghqqa/fastapi-boilerplate-demo

# 方式 B（推荐，透传桥的可信 run-context；env A 缺省时启用）：
# 路径 = 容器内 shared/ 镜像下、本次 run 的 write-once run-context.json。
# 只读挂载示例（compose 片段见 docker-compose.cr.example.yml）：
MERGEPILOT_CR_REPO_SCOPE_FILE=/root/.copaw-worker/reviewer/shared/projects/RUN_PROJ_PLACEHOLDER/run-context.json
