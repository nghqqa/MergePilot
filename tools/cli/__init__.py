"""MergePilot local CLI package (minimal, development preview).

The single implementation module is :mod:`tools.cli.mergepilot`; this package
exists only so the ``mergepilot`` console script (see pyproject.toml) can
import it as ``tools.cli.mergepilot:main``.
"""
