"""risk-classify Skill package: deterministic advisory L0/L1/L2 risk aggregator
that consumes a change context and a versioned declarative ruleset. Only-escalate,
never an authorization authority."""
